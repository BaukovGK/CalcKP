-- Версионирование шаблона изделия: редакция встроенного шаблона (кода),
-- которой собран состав дерева, — в снапшоте рядом с версией шаблона
-- технолога и версией прайса.

-- AlterTable
ALTER TABLE "EstimateSnapshot" ADD COLUMN     "builtinRevision" INTEGER;
