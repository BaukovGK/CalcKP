# Диздок: механика Калькулятора

**Документ:** техническое описание игровой механики расчёта стоимости изделия
**Связанные документы:** `ТЗ.md` (§5.5, §9), `Реверс_калькуляторов.md` (формулы и первоисточник)
**Версия:** 1.1 · 2026-09-08 (сверено с кодом ветки `dev`)

> **Пометка `⚠️ не реализовано`.** Документ местами опережает код: описывает
> принятые проектные решения, которых в системе ещё нет. Такие места оставлены
> намеренно — это план, а не поведение системы, — и помечены единообразно.
> Всё непомеченное сверено с кодом и работает.

---

## 1. Концепция

Инженер отвечает на вопросы опросного листа — система сама собирает полную спецификацию изделия с ценами и трудозатратами. Дальше инженер не «считает смету», а **тюнингует заготовку**: включает/выключает блоки, правит отдельные количества и цены. Система мгновенно пересчитывает итог и показывает цену продажи.

Ключевое ощущение: *«я управляю изделием, а не таблицей»*. Excel-предок требовал знать, какие ячейки трогать можно, а какие нельзя; здесь запрещённых ячеек нет — есть параметры, расчётные значения и явные ручки-переопределения.

**Антицель:** свободный табличный редактор, где каждый расчёт строится с нуля. Проверено реверсом: 6 реальных смет = один каркас + точечные правки.

---

## 2. Роли

Роли БД — `Role` (`backend/prisma/schema.prisma:11-17`).

| Роль | Что делает в механике | Мотивация |
|---|---|---|
| ENGINEER | Заполняет ОЛ, тюнингует расчёт, выпускает КП | Быстро получить корректную смету |
| MANAGER | Видит и правит чужие расчёты, следит за маржой. Согласование через статусы отменено (§10) | Уверенность, что маржа не потеряна |
| BUYER/ADMIN | Ведёт прайс НН | Актуальные цены = честный итог |
| TECHNOLOG | Правит справочники материализации на экране «Шаблоны» | Нормы соответствуют производству |
| VIEWER | Только просмотр расчёта — вкладка «Расчёт» в Битрикс24 | Видеть цифру, не имея прав её менять |
| Система | Считает всё, что можно посчитать | — |

---

## 3. Core loop

```
 ┌─────────────────────────────────────────────────────────┐
 │ 1. ОПРОСНЫЙ ЛИСТ                                        │
 │    выбрал тип изделия → заполнил параметры и флаги      │
 └────────────────┬────────────────────────────────────────┘
                  ▼  «Создать расчёт» (материализация шаблона)
 ┌─────────────────────────────────────────────────────────┐
 │ 2. РАСЧЁТ (заготовка)                                   │
 │    разделы созданы, количества посчитаны, цены          │
 │    подтянуты, ФОТ-спутники добавлены                    │
 └────────────────┬────────────────────────────────────────┘
                  ▼  цикл тюнинга (основной геймплей)
 ┌─────────────────────────────────────────────────────────┐
 │ 3. ТЮНИНГ                                               │
 │    вкл/выкл раздел · добавить/удалить строку ·          │
 │    override количества · override цены ·                │
 │    закрыть «красные» строки (нет цены)                  │
 │        └── каждое действие → мгновенный пересчёт итога  │
 └────────────────┬────────────────────────────────────────┘
                  ▼
 ┌─────────────────────────────────────────────────────────┐
 │ 4. ИТОГ: себестоимость → наценка → цена продажи         │
 │    → DRAFT → CALC · выпуск КП = снапшот (§10)           │
 │      → выгрузки: КП docx/pdf, Заявка на закупку         │
 └─────────────────────────────────────────────────────────┘
```

Петля второго порядка: изменение параметра ОЛ *после* создания расчёта → дерево сразу материализуется заново и сохраняется (ОЛ — основной экран изделия, §8.4), расчётные количества обновляются сами, а ручные overrides переносятся и, если легли на изменившееся расчётное, вспыхивают конфликтом, который дожидается экрана расчёта (§8.3).

---

## 4. Сущности

| Сущность | Суть | Аналог в Excel |
|---|---|---|
| **Изделие (deviceType)** | KNS (= КНС/ЛНС/ДНС, конструктивно одно изделие, вид НС — атрибут), EMK, KOL | Три листа-калькулятора |
| **Опросный лист (ОЛ)** | Анкета изделия: параметры + флаги. Единственный источник входов | Листы ОЛ_* |
| **Узел каталога** | Единая рекурсивная сущность: **компонент** = узел-лист (обечайка, днище, патрубок…), **сборка** = узел с детьми (сборки из сборок). Каталог описан в `Библиотека_компонентов.md`. В коде узлы двух видов: встроенные — формулы кодом, реестр `ntt-calculator/src/engines/code-nodes.ts`, — и узлы технолога из редактора шаблонов — строки с формулами-выражениями над параметрами (`engines/node-def.ts`); сборок из сборок пока нет. Компонент хранит код узла (A1…D5) и у узла технолога — его версию (`nodeCode`, `nodeVersion`) | Повторяющийся блок строк / раздел |
| **Шаблон изделия** | Сборка верхнего уровня с биндингами параметров на поля ОЛ; в расчёт попадает материализованной копией. Данные: разделы по порядку и ссылки на узлы (`engines/template-def.ts`). Встроенный шаблон (версия 0) — `BUILTIN_TEMPLATES` в `engines/code-nodes.ts`; технолог публикует свои версии в редакторе шаблонов (`ProductTemplate`/`ProductTemplateVersion` в `schema.prisma`). Версия шаблона фиксируется в дереве (`CalcTree.templateVersion`) и в снапшоте (`EstimateSnapshot.templateVersion`) | Каркас листа калькулятора |
| **Раздел** | Сборка верхнего уровня шаблона: блок спецификации с флагом включения и подытогами | «1. Корпус», «5. Напорный трубопровод»… |
| **Строка** | Атом расчёта: материал / работа / ФОТ | Строка листа |
| **Прайс (НН)** | Каталог цен, ключ = Категория+Наименование+ЕИ (`PriceItem`). Версионируется: новую `PriceListVersion` с номером MAX+1 создают импорт xlsx и ручная правка цены (`backend/src/utils/price-version.ts`) | Лист НН |
| **Справочники** | Веса ГРП-труб (DN;PN;SN) `PipeWeight`, ПЭ-трубы `PePipe`, инженерные матрицы `EngineeringMatrix` (корпус, эллиптические днища), нормы патрубков `NozzleNorm`, каталог насосов `Pump` (61 модель Vandjord VSL). ⚠️ **не реализовано:** BOM-комплекты узлов и каталог АШУ — таких таблиц в `schema.prisma` нет | Вес трубы, Для расчетов, Вспомогат. таблицы, Справка АШУ |
| **Снапшот** | Замороженная копия расчёта + версия прайса | Файл «АКТ» |

---

## 5. Механика строки (атом системы)

Каждая строка — конечный автомат с двумя независимыми каналами: **количество** и **цена**.

### 5.1 Канал количества

```
qtyCalc  ──(формула шаблона от ОЛ и справочников)──►  read-only, серый
qtyManual ──(ввод инженера)────────────────────────►  приоритет, синий
qty = qtyManual ?? qtyCalc
     × флаг раздела (выкл → 0)
     × кол-во корпусов (тираж)
```

Правила:
- `qtyCalc` никогда не редактируется и никогда не теряется: сброс override возвращает расчётное значение (кнопка «↺ к расчётному»).
- Ввод `qtyManual` разрешает произвольные арифметические выражения (`1.55*2+2.88*2`) — в Excel так считают длины; выражение сохраняется, отображается результат.
- **Текст в числовые поля не попадает** — ни в расчёте, ни в ОЛ (`ntt-calculator/src/utils/numeric-input.ts`, ставится на документ в `main.ts`). В поле с классом `num` или атрибутом `data-numeric` печатаются только символы грамматики выражений: цифры, `,` `.`, пробел-разделитель разрядов, `+ − * /`, скобки, `=` в начале. Буква с клавиатуры не вставляется; из вставки лишнее вычищается, цифры остаются. Если значение и после этого не разбирается («2**3»), поле подсвечивается красным с подсказкой, а ОЛ не сохраняется, пока его не исправят («не сохранено: исправьте поля, выделенные красным»). Раньше такой ввод молча становился «пусто»: цена трубы пропадала из расчёта, а инженер видел в поле свой текст.
- Строка без формулы (добавленная вручную) имеет только `qtyManual`.
- **Непринятый ввод не пропадает молча** (с 11.09.2026, План_устранения 1.5): выражение, которое не разобралось, и количество меньше нуля в расчёт не идут — там расчётное, — но остаются в ячейке как введены, с красной рамкой и примечанием «не разобрано · в расчёте 12» или «меньше нуля не принято · в расчёте 12»; ↺ убирает ввод. Прежде неразобранное молча откатывалось к расчётному, а «-5» уменьшало себестоимость (`manualQty`, `ntt-calculator/src/engines/row.ts`). Скидка — через наценку, а не строкой с минусом (решение Р4). Такие строки собирает фильтр «✕ ввод не принят» и счётчик проблем в шапке.

### 5.2 Канал цены

```
priceCatalog ──(поиск в прайсе по ключу C+N+U)──►  read-only, серый
priceManual  ──(ввод инженера)──────────────────►  приоритет, синий
price = priceManual ?? priceCatalog
sum   = qty × price
```

Правила:
- Ручная цена меньше нуля и неразобранная не принимаются: ячейка остаётся прежней и объясняет отказ («цена не принята: меньше нуля»). Отрицательная ручная цена из расчёта, сохранённого раньше, в расчёт не идёт — там цена прайса (решение Р4, План_устранения 1.5).
- Промах по прайсу (`priceCatalog = null`) и нет `priceManual` → строка **красная**, сумма = 0, счётчик «незакрытых» строк в шапке. Типовой случай — труба/муфта корпуса: цена всегда договорная, вводится вручную.
- Ключ C+N+U сравнивается **в каноническом виде** (с 10.09.2026): одиночные пробелы, без пробелов по краям, кириллица на месте латинских двойников («ГOCT» → «ГОСТ», «108x8» → «108х8», «M12-7H» → «М12-7Н»). Правила — `backend/src/utils/price-name.ts`, копия во фронтенде — `engines/price-name.ts`, общие примеры — `price-name.vectors.json`. Их применяют импорт прайса, миграция `20260910120000_normalize_price_names` (привела 111 уже сохранённых наименований), поиск цены в расчёте и сопоставление строк при пересборке: невидимая разница больше не делает строку «красной» и не теряет ручные правки.
- Прайс ведётся выгрузкой и загрузкой листа «НН» (экран «Реестр цен»). Выгрузка — книга в раскладке листа мастер-шаблона плюс «Поставщик» и лист «Проверка» (позиции без цены и с подозрительными ценами: цена не сходится с ценой без скидки, лист за штуку расходится с ценой за м² больше чем на 40 %). Загрузка — в два шага: предпросмотр (новые позиции, изменения цен с процентом, повторы ключа с обеими ценами, пустые цены, исправленные наименования) и только потом запись. Позиции, которых нет в файле, не удаляются; **пустая цена в файле цену базы не стирает**; поставщик читается, если в файле есть его колонка. Правила — `backend/src/utils/price-import.ts`; консольный вариант — `node dist/tools/import-prices.js <книга.xlsx> [--dry-run]`.
- Материализация **фиксирует** применённую цену: `priceCatalog` пишется один раз, при вставке узла (`ntt-calculator/src/engines/template-kns.ts:197-198`), и хранится в дереве вместе с номером версии прайса (`CalcTree.priceListVersion`). Активную версию сервера отдаёт `GET /api/refs/price-version` (MAX по `PriceListVersion`, `backend/src/routes/refs.routes.ts:47`); топбар показывает версию, из которой построен расчёт.
- `priceManual` при повторной материализации (правка ОЛ) переносится в новое дерево и не трогается — `ntt-calculator/src/stores/calcTree.ts:292`.
- **Пересчёт по новой версии прайса (с 11.09.2026).** Импорт прайса и правка цены на экране прайса (она тоже создаёт версию — `utils/price-edit.ts`) меняют строчные цены только у новых расчётов — старый расчёт держит цены своей версии. Если действующая версия новее `CalcTree.priceListVersion` **и цены строк расчёта с ней расходятся** — у строки сменится цена прайса или сдвинется ставка ФОТ (`hasPriceDrift`, `engines/reprice.ts`), — экран расчёта показывает под фильтрами плашку «Цены строк — из прайса НН v2, а действует v5» с предпросмотром: сколько строк сменит цену, как сдвинутся ставки экономики, себестоимость и цена продажи (`repricePreview`, `stores/calcTree.ts`). Кнопка «Пересчитать по прайсу v5» берёт цену каждой строки заново по той же тройке C+N+U (`repriceTree`, `ntt-calculator/src/engines/reprice.ts`), ставит дереву действующую версию и сразу сохраняет расчёт. Правила:
  - у строки со сменившейся ценой прежняя запоминается в `priceCatalogPrev` — отметка «было … ₽» в примечании и янтарная рамка цены, фильтр «₽ цены изменились», ✓ принимает новую цену, ↶ оставляет прежнюю ручной; «принять все» снимает отметки разом. После двух пересчётов подряд помнится самая ранняя цена, как у конфликта количества (§8.3);
  - ручная цена остаётся ручной, но отметка ставится: прайс под ней сдвинулся;
  - договорная труба (`priceBinding` pipePrice / servicePipePrice) не трогается — её цена в поле ОЛ;
  - позиции, которой в новом прайсе нет, цена остаётся прежней, строка считается в сводке «нет в новом прайсе»: обнулить цену значило бы молча заблокировать КП;
  - ФОТ-спутники получают новую ставку без отметок — сдвиг ставки один на все и показан в сводке.

  Версия прайса растёт с каждой правкой цены, поэтому решает расхождение, а не номер: правка позиции, которой в расчёте нет, пересчитать его не зовёт. Такое дерево при сохранении молча переходит на действующую версию (`catchUpPriceVersion`, `stores/calcTree.ts`) — пересчитывать нечего, а подпись «прайс vN» в КП и снапшоте не отстаёт от прайса, по которому цены верны.

  Пересборка по правке ОЛ тоже берёт цены из действующего прайса — теперь и она отмечает сдвинувшиеся цены (`priceMarkAfter` в `reconcileTrees`), а не меняет их молча. Ставки экономики (ФОТ, накладные, ацетон, СИЗ) пересчёт берёт из того же прайса, что и цены строк, и показывает сдвиг каждой; в дереве они фиксируются вместе с ценами (§9).

### 5.3 Классификация

- `category` — из фиксированного списка 16 категорий. Категория определяет флаг «Закупка»: категории «Собственное производство», «Работы», «ФОТ» — не закупка, остальные 13 — закупка (попадают в Заявку).
- `unit` определяет физический смысл: `кг` — формовка/ламинат (масса участвует в расчёте ФОТ и ацетона), `чел. ч` — труд (участвует в накладных), прочие — штучные материалы.
- Наименование трубы корпуса генерируется: `Труба {материал}-К {DN}-{PN_ОЛ}-{SN}`. У КНС и КОЛ материал всегда `СК/НПС` (`pipeGradeName`, `ntt-calculator/src/engines/survey-kns.ts`; `buildKolShell`, `template-emk-kol.ts`); у ЕМК он зависит от типа среды — `СК/ВЭС` для химстойкой, иначе `СК/НПС` (`tankMaterial`, `survey-emk-kol.ts`; `emkPipe`, `template-emk-kol.ts`). Трубы шахты, горловины и гильз — `СК/НПС-К` своей марки, как вписано в листах: SN 5000 у шахты, 2500 у горловины и гильз, PN 0,1. В марку идёт **обозначение** жёсткости: по ТТ МВК заказчик видит 8000/12000 вместо расчётных 5000/10000 (`snDesignation`, §7.2).
- При смене параметров ОЛ дерево рематериализуется, и наименование вместе с ключом прайса перегенерируется: `load()` сравнивает `surveyRev` с `treeSurveyRev` и строит свежее дерево, перенося в него ручные правки (`ntt-calculator/src/stores/calcTree.ts:143-161`).
- Гильза патрубка отдельной штучной строкой **не** генерируется. У всех трёх изделий — как в их листах: гильза — отрезок трубы `Труба СК/НПС-К {Ø}-0,1-2500` по 0,5 м на патрубок (меньше DN 200 — «Ручная формовка патрубка», 0,5 кг на патрубок), её товарный вид и «Ламинирование патрубка к корпусу» Мф·3/10 с k ФОТ 1 (`buildSleeveNozzles`, `ntt-calculator/src/engines/template-kns.ts`); труба — продукция завода, строка «красная» до ввода цены. Фланцевый патрубок КНС под задвижку — отрезок трубы 0,3 м (меньше DN 300 — ручная формовка), фланец и ламинирование по норме патрубка (`buildKnsInletFlange`). До DN 300 включительно гильза формуется — «Формовка гильз» Мф(Ø гильзы) × кол-во с k ФОТ 1 — у напорных патрубков КНС (через неё протягивается напорная труба малого диаметра) и у стеклопластиковых патрубков ёмкости под стеклокомпозитную трубу; лист здесь ставит ручную формовку 0,5 кг или отрезок трубы (уточнения 11.09.2026). Стеклопластиковый патрубок ёмкости получает ещё «Ручную формовку стеклокомпозитного фланца» Мф фланца(DN) × кол-во с болтовым комплектом, у колодца и КНС вместо гильзы ставится муфта «Муфта-2 СК/НПС-К DN-1» на патрубок (та же «Муфта-1» без центрального ограничителя, цена договорная) и ламинируется к корпусу: у КНС трубы заводятся внутрь через муфту или гильзу, фланцы — уже внутри. Болтовой комплект (`flangeBoltRows`: болт по норме DN, две шайбы, гроверная и гайка на болт) нужен каждому фланцевому соединению: фланцу стеклопластикового патрубка, патрубку КНС под задвижку и стыкам напорного трубопровода — там соединений столько же, сколько свободных фланцев нитки. До 11.09.2026 «Формовка гильз» стояла у всех патрубков КНС. ⚠️ **не реализовано:** наименование вида `Стеклокомпозитная гильза Ø{D}`.

---

## 6. Механика ФОТ-спутников

К каждой производственной операции (строка «Собственное производство» с ЕИ=кг) система автоматически прикрепляет строку-спутник «ФОТ»:

```
трудоёмкость (чел.ч) = ROUNDUP( масса_операции_кг × k , 0.1 )

k = 0.28  если наименование операции содержит «мех»    (механическая формовка)
k = 0.56  если содержит «ламин»                        (ламинирование)
k = 1.0   иначе                                        (ручная формовка и особые случаи)
```

- Спутник реагирует на итоговое `qty` родителя (после override), не на `qtyCalc`; тираж к спутнику не применяется — он уже пришёл из массы родителя (`recalcFotSatellites`, `ntt-calculator/src/engines/fot.ts:59-78`).
- Цена спутника = позиция прайса «ФОТ / ФОТ / чел. ч» (актуальная ставка 1207,8 ₽) — фиксируется при материализации (`operationWithFot`, `ntt-calculator/src/engines/template-kns.ts:208-228`).
- Спутник можно переопределить вручную (H-канал), но нельзя удалить отдельно от родителя.
- Отдельные строки узлов каталога задают коэффициент явно (особые ламинирования с k=1.0) — коэффициент хранится в строке узла, эвристика по подстроке — значение по умолчанию.

---

## 7. Механика разделов

### 7.1 Каркас KNS (эталон)

Каркас встроенного шаблона — `KNS_SECTIONS` (`ntt-calculator/src/engines/template-kns.ts`), узлы по разделам — `BUILTIN_TEMPLATES.KNS` (`engines/code-nodes.ts`). Шаблон технолога может переставить разделы и узлы (§7.3); ниже — встроенный. В скобках — узлы, которые материализуются из ОЛ сегодня:

1. Корпус (A1 обечайка + товарный вид + подготовка трубы, A2 днище формованное + ламинирование, A5 патрубки с гильзами, A6 фланцевый патрубок под задвижку, A7 кабельный ввод, A9 теплоизоляция, A10 монтажные петли, D4 дробилка, D3 корзина)
2. Лестница (B1: тетивы, ступени, изготовление, монтаж)
3. Перекрытие, площадка, несущие балки (B2 перекрытие и рама, B6 анкерное крепление против всплытия, D1 крепление насосного оборудования, D5 подъём насосов)
4. Вентиляционный стояк (C1)
5. Напорный трубопровод (C2) ← самый «живой»: 55–109 строк в реальных файлах; из ОЛ выводятся комплект нитки по DN, обвязка датчика давления, аварийная линия и нормативы трудоёмкости, компоновку (метраж, отводы, тройники) инженер снимает с чертежа
6. Крепёж (C3)
7. Оборудование и запорная арматура (C4 арматура и её монтаж, D1 насосы, трубные муфты, поплавки и монтаж, D2 шкаф управления, датчики, расходомер, D5 тренога, таль, газоанализатор)

Какие строки листа КНС за этим стоят и что сознательно не перенесено — `doc/Шаблон_КНС_разбор.md` §4 (построчная сверка с образцом, 10.09.2026).

EMK — 8 разделов (`EMK_SECTIONS`, `ntt-calculator/src/engines/template-emk-kol.ts`): + автогабариты из объёма (у горизонтальной — за вычетом объёма днищ), у горизонтальной — два днища по концам трубы (эллиптические или цилиндрические, тумблер ОЛ) и крепление к бетонному основанию ремнями вместо перекрытия и анкеров, шахты обслуживания по числу из ОЛ — с люками, вентстояками и приставными лестницами на каждую, отдельный раздел 2 «Корзина», раздел 8 «Оборудование» из ОЛ (задвижка на подводящем, насосы, шкаф, датчик уровня, тренога, таль, газоанализатор). KOL — 7 разделов (`KOL_SECTIONS`, там же): без насосов и напорного трубопровода, + горловина своей трубой и её люк, тоже с разделом 2 «Корзина»; «Крепёж» и «Оборудование» — каркасы для ручных строк. Общие узлы (лестница, перекрытие, вентстояк, оборудование станции) переиспользуются из шаблона КНС. Построчная сверка с образцами листов — `doc/Шаблон_ЕМК_КОЛ_разбор.md`.

### 7.2 Триггеры включения (флаг ← поле ОЛ)

| Блок | Триггер | Поведение |
|---|---|---|
| Теплоизоляция | ОЛ «теплоизоляция» + глубина ТИ | Узел A9 материализуется с `enabled = флаг ОЛ`; выкл → qty всех строк блока = 0 (`ntt-calculator/src/engines/template-kns.ts:363-372`) |
| Корзина сороудерживающая | ОЛ «дробилка/корзина» = «корзина» или «обе»; у КНС одно из двух есть всегда, и «нет» старых листов — корзина (`knsBasketOn`) | Узел D3 (`ntt-calculator/src/engines/basket-grinder.ts`, `buildBasket`): у КНС — в разделе 1 «Корпус», у ЕМК и КОЛ — отдельный раздел 2. Состав по эталону: уголок 10 м, перфолист 2 м², труба направляющих ROUNDUP(2·H/1000), цепь ROUNDUP(H/1000 + 1), где H — глубина лотка подводящего; такелаж у КНС — кольца и скобы, у ЕМК и КОЛ — пруток «Круг 5 мм» и карабин на 2 м цепи; работы 6 + 5 чел.ч при DN < 2500, 7 + 6 — при большем, направляющие 2 + 2. У КНС при корзине вместе с дробилкой труба направляющих корзины = 0 (лист КНС, строка 105). Без глубины лотка строки цепи и направляющих остаются пустыми с примечанием |
| Дробилка | ОЛ «дробилка/корзина» = «дробилка» или «обе» | Узел D4 (`buildGrinder`), только КНС и КОЛ — в разделе 1: формовка канала 4,7 м² × 7 мм × 1850 кг/м³ (ФОТ 0,28), ламинирование площадки × 3/10 (ФОТ **1**), уголок направляющих ROUNDUP(2·H/1000), цепь 5 мм, подвес; у КНС ещё труба рамы ROUNDUP(2·DА/1000), кольца и «Монтаж направляющих дробилки» 2 чел.ч. Сам измельчитель (ПОТОК П-200…П-800) эталон не материализует — подбирается по притоку и добавляется в «Оборудование». В листе ёмкости узла дробилки нет, у ЕМК тумблер на расчёт не влияет |
| Днища горизонтальной ёмкости (ЕМК) | ОЛ «Днища — 2 шт., по концам трубы»: эллиптические / цилиндрические; тумблер виден только при горизонтальном расположении | Узел A3 (`buildEmkBottoms`, `ntt-calculator/src/engines/template-emk-kol.ts`). Длина трубы горизонтальной — из объёма за вычетом двух днищ `π·D³/15` (лист «для шапки»). **Эллиптические:** «Механическая формовка эллиптических днищ» = 2 × масса ячейки матрицы «Формовка эллиптических днищ» по DN и строке длины трубы (`matrixLengthBucketMm`: до 3 м — «До 3 м», шагом 0,5 м до 7 м, длиннее — «До 12»; индекс строит `ensureContext`, `ellipticBottomOf`), ФОТ 0,28; «Ламинирование эллиптического днища к корпусу» = 2 × Мс (DN трубы, минимальное PN), ФОТ 0,56. **Цилиндрические:** трубы корпуса +1,5 м (эталон I13, `CYLINDRICAL_BOTTOMS_PIPE_MM`), «Ламинация днища (косые и центральный стыки)» = (Мс/0,707 + Мс/2)·2, ФОТ 0,56. Габаритная длина в обоих случаях — длина трубы + 1,5 м (в листе у подземной — длина трубы, у наземной — + DN/2). Вне сетки матрицы или без Мс строка рождается пустой с примечанием. Лист расчёта матрицу масс не читает — при «старом способе» его строка 25 считает количество формулой стыков (Вопросы_заводу §6е). Старые расчёты без поля — эллиптические. Вертикальная ёмкость — плоское дно без фальшпола («Механическая формовка плоского днища» и «Ламинирование плоского днища»), тумблер ни на что не влияет |
| Задвижка на подводящем | у КНС — всегда (уточнение завода 11.09.2026; прежде флаг ОЛ «арматура на подводящем»), с фланцевым патрубком под неё (A6) | Кол-во задвижек = подводящих: `gateValveCount` (`ntt-calculator/src/engines/survey-kns.ts`), строка в узле C4 вместе с монтажом 1 чел.ч на штуку. Задвижка — шиберная с удлинённым штоком, как в листе (строка 408): длина штока от оси трубы = глубина лотка подводящего − DN/2 (`inletGateValveName`, `template-kns.ts`). В прайсе её нет — строка «красная» до ввода цены |
| Горловина (KOL) | ОЛ «горловина» + d,h | Своя труба `СК/НПС-К d-0,1-2500` высотой h + возвышение (эталон N5), товарный вид, муфта, ламинирование к корпусу Мф(d), подготовка трубы, прорезка — узел A8 (`buildKolNeck`); труба корпуса — рабочая часть (H5), без горловины — рабочая часть с возвышением (`computeKolGeometry`, `survey-emk-kol.ts`). Полная глубина H5 + N5 — для SN и лестницы. Крышка люка горловины — узел B2 «Люк горловины» в разделе 4 (`buildKolHatches`, масса по площади `neckCoverMassKg`) |
| Шахты обслуживания (EMK) | ОЛ «шахта» + число, d, h | На каждую шахту: труба `СК/НПС-К d-0,1-5000` на высоту шахты, товарный вид, муфта, ламинирование к корпусу Мф(d), подготовка трубы, прорезка (узел A8, `buildEmkShaft`); люк с 2 ручками, замком и концевым выключателем (`buildEmkHatches`), вентстояк, приставная лестница; утепление — по шахтам (`emkInsulation`). Число шахт — поле ОЛ, пусто — одна |
| Насосы | ОЛ кол-во раб + рез + запасных | Насосов в поставку = раб + рез + запасные; трубных муфт (АТМ) = раб + рез; монтаж насосов 2, муфт 3 чел.ч на установленный насос; поплавков = раб + рез + 2 (`floatSwitchCount`) с кабелем по высоте станции. Раздел 3: направляющие — **материал**, труба из паспорта насоса `L·(раб+рез)·2` м, работы по 2 чел.ч на насос, рама насосов 6/5 или 7/6 чел.ч по порогу DN 2500; цепь подъёма `ROUNDUP((раб+рез)·(H + 1))` (`buildPumpMounting`, `ntt-calculator/src/engines/station-equipment.ts`). До 10.09.2026 направляющие считались трудом «0,25 чел.ч/м» в разделе 5 — такого норматива в эталоне нет |
| Высота станции | ОЛ «Возвышение» + подземная часть | `stationHeightM` = Нподз + возвышение (`H5` листа): от неё цепь подъёма насосов, высота тали (6/9/12/18 м) и длина кабеля поплавков (10–40 М) и датчиков (10/20 м) — наименьший типоразмер прайса, не меньший высоты; больше самого крупного — «красная» строка с обобщённым наименованием |
| Марка трубы по ТТ МВК | ОЛ «По ТТ МВК» | Меняет **обозначение** жёсткости в наименовании: 5000 → 8000, 10000 → 12000 (`snDesignation`, `ntt-calculator/src/engines/survey-kns.ts:161,171-173`). Вес и трудоёмкость считаются по расчётной жёсткости — изделие то же |
| ⚠️ **не реализовано:** Фальшпол | флаг раздела | Признака фальшпола нет ни в форме ОЛ (`KnsSurveyForm`), ни в параметрах материализации (`KnsSurveyParams`). Днище КНС всегда одно: «Механическое формованное дно» + «Ламинирование дна к фальшполу» (`ntt-calculator/src/engines/template-kns.ts:277-300`); альтернативы «плоское днище» нет. Развилка днищ есть только у ЕМК: плоское у вертикальной, эллиптические или цилиндрические у горизонтальной (тумблер ОЛ) — от размещения, а не от фальшпола |
| Лестница (ЕМК, КОЛ), вентстояк (ЕМК) | ОЛ «Лестница», у ёмкости ещё «Вентиляция» | Узлы B1 и C1 материализуются с `enabled = ответ ОЛ` (`surveyToggled`, `buildLadder`/`buildVent` в `ntt-calculator/src/engines/template-kns.ts`); ручное переключение узла в расчёте живёт до смены ответа в ОЛ, как у теплоизоляции. У старых деревьев узлы считаются построенными с ответом «да» — так они и строились (`ALWAYS_ON_BEFORE_SURVEY`, `stores/calcTree.ts`), поэтому выключенная вручную лестница не включается ответом «да». Длина лестницы ёмкости — по эталону (I114 листа «Калькулятор ЕМК», `emkLadderHeightMm`): у вертикальной во всю высоту корпуса, у горизонтальной — DN + высота шахты; колодца — полная глубина с горловиной. У КНС вопросов нет, узлы всегда включены. В эталоне ответы ОЛ с листом расчёта не связаны: разделы включает флаг «Да/Нет» в самом листе (C112, C208) |
| Запорная арматура ЕМК | ОЛ «Запорная арматура на подводящем» | Узел C4 «Задвижка на подводящем» (`buildEmkValves`) по ответу ОЛ: шиберная DN подводящего с удлинённым штоком по числу подводящих, шток = лоток − DN/2 (в листе ёмкости — `длина лестницы − лоток/2`, Вопросы_заводу §6и), монтаж 1 чел.ч; строка «красная» до ввода цены. Задвижки и клапаны напорной стороны лист вписывает руками — их узел не строит |
| ⚠️ **не реализовано:** Запорная арматура КОЛ | ОЛ «Запорная арматура на подводящем» | Справочно, раздел «Оборудование и запорная арматура» колодца заполняется вручную: в листе задвижка DN100 с двумя фланцами и борт-шайбами, количества вписаны руками (строки 240–246) |
| Лестница нерж + приставная | ответ ОЛ «Лестница» у ЕМК и КОЛ | У ЕМК и КОЛ вместе с нержавеющей строятся приставные алюминиевые «14 ступеней 3,98 м» — у ёмкости по одной на шахту, у колодца одна, монтаж 2 чел.ч на штуку (`buildLadder`, `portable`), как в их листах. У КНС — только нержавеющая: приставные в её образце вписаны руками |
| ⚠️ **не реализовано:** Крышка стеклокомпозит ↔ металл | выбор в разделе | Развилки нет: крышки люков ЕМК и КОЛ — стеклокомпозитные (узел «Люк»), нержавеющего перекрытия колодца (строки 146–152 листа) нет |
| ⚠️ **не реализовано:** МВК-комплект | ОЛ «По ТТ МВК» | Флаг доходит до шаблона (`ntt-calculator/src/stores/calcTree.ts:232`), но влияет только на обозначение SN в марке трубы. Строк «поручень», «откидные люки», «перехватывающее устройство» в движке нет; вентиляция одна и безусловная — «Вентиляционный стояк ПЭ Ду110» (`ntt-calculator/src/engines/template-kns.ts:520-521`). Экран ОЛ при этом показывает блок «МВК-комплект» (`ntt-calculator/src/views/SurveyKnsView.vue:393`) |
| Автоматика КНС | ОЛ «Шкаф управления», «Датчики давления», «Датчики уровня», «Расходомер» | Каждый тумблер ведёт свой узел D2 раздела 7 (`buildAutomation`, `ntt-calculator/src/engines/station-equipment.ts`), выключенный — «призрак»: шкаф управления (тип и пуск — в наименовании; цена вручную, в прайсе шкафов нет) + монтаж 6 чел.ч; датчик давления на каждый напорный патрубок; погружной датчик уровня с футляром ПЭ Ø110 на длину корпуса; расходомер DN напорного на каждый напорный. Монтаж — 1 чел.ч на прибор. Тренога, таль и газоанализатор (D5) — всегда, как в листе |
| Оборудование ёмкости | ОЛ «Насосное оборудование», «Марка», «Шкаф управления», «Датчики уровня» | Раздел 8 по листу (строки 260–284): насосы раб + рез с маркой из ОЛ, трубные муфты, поплавки раб + рез + 2, монтаж 2 / 3 / 1 чел.ч (`buildEmkPumps`, только при насосах); шкаф управления и погружной датчик уровня с футляром — по ответам ОЛ (`buildEmkAutomation`); датчиков давления и расходомера ОЛ ёмкости не спрашивает — их узлы выключены; тренога, таль и газоанализатор — всегда (`buildEmkService`). Кабели и таль — по высоте лестницы. Старые параметры без этих полей берут ответы из формы ОЛ (`emkParamsWithForm`, `stores/calcTree.ts`) |
| ⚠️ **не реализовано:** Датчики / ШУ колодца | ОЛ, по-элементно | В листе колодца строк шкафа и датчиков нет — раздел «Оборудование» колодца пуст |

Выключенный раздел не удаляется: `toggleSection`/`toggleComponent` меняют только флаг `enabled`, `qtyManual`/`priceManual` сохраняются, обнуление количества — вычисление: флаг раздела и компонента сворачивается в предикат по строке (`sectionEnabledFor`, `ntt-calculator/src/engines/template-kns.ts:775`), а само обнуление делает `resolveQty` (`ntt-calculator/src/engines/row.ts:34`). Включение обратно восстанавливает всё, включая overrides.

Но **видимы такие строки не по умолчанию**: фильтр таблицы убирает их вместе с подзаголовком компонента, пока не включён чекбокс «выключенные» (`ntt-calculator/src/views/CalculatorTreeView.vue:54` и :418, инициализация `ghosts: false` — :341). Свёрнутого «призрачного» состояния как отдельного режима отображения нет.

### 7.3 Шаблон технолога: версия, пересборка, узлы каталога

С 11.09.2026 состав изделия — шаблон-данные (`engines/template-def.ts`): встроенный (версия 0, `engines/code-nodes.ts`) либо опубликованный технологом в редакторе шаблонов (Библиотека §6.2). Калькулятор берёт действующий шаблон вместе со справочниками (`GET /api/refs/templates`, `utils/materialize-context.ts`); сервер без шаблонов — изделие собирается встроенным.

- **Версия состава** — два числа. Версия шаблона технолога (`CalcTree.templateVersion`; 0 — встроенный шаблон) и **редакция встроенного шаблона** (`CalcTree.builtinRevision`) — версия кода: формул и состава встроенных узлов, встроенного шаблона, исполнения узлов каталога (`engines/builtin-revisions.ts`). Редакцию поднимает релиз, меняющий то, что программа строит из ОЛ; забыть её поднять не даёт тест-страж: он сверяет отпечаток сборки встроенного шаблона на наборе ОЛ (`engines/builtin-fingerprint.ts`) с отпечатком последней редакции и падает, если сборка изменилась, а редакция — нет. Топбар расчёта показывает обе версии («шаблон v2 · ред. 3»); если действует другая версия шаблона или код новее редакции расчёта, под фильтрами — плашка с кнопкой «Пересобрать» (`rebuildByActiveTemplate`, `stores/calcTree.ts`): свежая материализация из сохранённого ОЛ, перенос правок по §8.3, сразу сохранение. Для новой редакции плашка перечисляет из журнала, что изменилось; дерево без редакции собрано до её учёта и тоже предлагает пересборку. Правка ОЛ пересобирает по действующему шаблону сама.
- **Номер раздела** — его место в шаблоне; шаблон технолога может переставить разделы и перенести узлы — перенос правок это учитывает (§8.3).
- **Справочники под расчётом** (с 11.09.2026, План_устранения 3.6). Сборка записывает в дерево, какие значения справочников она прочла: вес трубы по (DN; PN; SN), нормы патрубка, Мс на стыке, массу эллиптического днища, версию узла каталога (`CalcTree.refsUsed`, `engines/refs-used.ts`). При открытии расчёта те же обращения повторяются по нынешним справочникам; разошлось хоть одно — плашка «Справочники изменились после сборки расчёта» с тем, что именно изменилось («вес трубы DN1000 PN0,6 SN10000: 120 → 125»), и кнопка «Пересобрать по нынешним» — ручные правки переносятся, как при правке ОЛ. Правка строки справочника, которой расчёт не касался, его не тревожит. Цены прайса сюда не входят — у них своя версия и пересчёт (§5.2). Деревья, собранные раньше, записи не имеют и не сверяются.
- **Узел каталога в расчёте.** «+ Компонент из каталога…» → вкладка «Узел каталога»: параметры узла (умолчания; параметр, названный как поле ОЛ, — значение из ОЛ этого расчёта), предпросмотр строк с ценами — и узел ложится в раздел целиком (`addCatalogNode`). Компонент ручной (`custom-…`): строки правятся и удаляются, как добавленные вручную (операция — вместе с ФОТ-спутником), узел убирается кнопкой «✕ убрать узел»; пересборка переносит его целиком. Id строк свои (`cn-…`): счётчик материализации после перезагрузки страницы начинается заново и иначе повторил бы их.

---

## 8. Механика пересчёта (reactivity)

### 8.1 Граф зависимостей

```
ОЛ-параметр ──► qtyCalc строк ──► qty ──► масса/часы ──► ФОТ-спутники
                                   │                        │
Прайс ──► priceCatalog ──► price ──┴──► sum ──► подытоги раздела
                                                  │
                                     итоговый блок (§9) ──► цена продажи
```

Любое изменение распространяется вниз синхронно, за один тик; итог всегда консистентен.

### 8.2 Что на что влияет (примеры)

- DN корпуса → вес трубы (справочник DN;PN;SN), масса днища (`bottomMassKg`), перекрытия (`topSlabMassKg`), теплоизоляции (`insulation`, у КНС `knsInsulation`), наименование трубы, чел.ч «товарного вида» (`marketableAppearanceHours`, DN/1300) и подготовки трубы (`pipePrepHours`, DN/1200) — `ntt-calculator/src/engines/formulas.ts`; тип монтажных петель (DN < 2000 — простые, от 2000 — усиленные, `mounting-loops.ts`); нормы рамы перекрытия и рамы насосов (`frameHours`, порог DN 2500).
- Глубина L → длина трубы, теплоизоляция, лестница (`ladder`), направляющие (`pumpGuidesM`), анкеры (`anchorCount`) — `ntt-calculator/src/engines/formulas.ts:233,250,260`.
- DN патрубка → диаметр гильзы `CEILING(DN+100; 50|100)` (`sleeveDiameter`), нормы формовки патрубка (`NozzleNorm`), прорезка отверстия 0,5 чел.ч/м окружности (`cutoutHours`, `ntt-calculator/src/engines/formulas.ts:223`), норма болтов фланцевого соединения.
- Расход/напор → гидравлика → рекомендация насоса. Работает сквозь весь поток: опросный лист по притоку, расчётному напору и числу рабочих насосов спрашивает `POST /api/pump-station/select-pump` (`composables/usePumpSelection.ts`), подобранная марка уходит в `derived.pumpModel`, оттуда в параметры материализации и в наименование строки раздела 7 — «Насос {марка}», а дальше в спецификацию КП. Отбор идёт по паспортной кривой с окном запаса по напору 0,5…2,0 м: запас страхует не рост притока (он уже в требуемом напоре), а несовпадение реального насоса с паспортом. По умолчанию подставляется наименьший достаточный, но экран даёт список всех подходящих моделей с их рабочими точками — инженер выбирает другую, включая отсечённые по минимальному запасу (они показаны отдельной группой). Поле «Марка насосов» в ОЛ — ручное переопределение подбора по общему правилу «расчёт + override» (§5.1). Цена насоса остаётся договорной: в прайсе НН категория «Насосы, АТМ» пуста, поэтому строка по-прежнему «красная». Диаметр напорного трубопровода (`POST /api/pump-station/discharge-pipe-diameter`) показывается подсказкой и в поле DN автоматически не подставляется: от DN зависят наименования строк, и молчаливая правка увела бы за собой ручные цены при рематериализации (§8.3).

### 8.3 Конфликт «ОЛ изменился после тюнинга»

Правило: **расчётные значения обновляются всегда, ручные — никогда молча**. Дерево материализуется заново, overrides переносятся (`reconcileTrees`, `ntt-calculator/src/stores/calcTree.ts`). Строка, где `qtyManual` перекрывает изменившееся расчётное значение, получает `qtyCalcPrev` — расчётное «было» — и попадает в `conflictIds`; инженер решает по каждой: «Оставить моё» (`keepOverride`) или «Принять новое» (`dropOverride`), оба гасят `qtyCalcPrev`.

`qtyCalcPrev` хранится **в дереве**, а не в памяти экрана: пересчёт теперь запускает сам ОЛ (§8.4), и конфликт, рождённый там, иначе не дожил бы до экрана расчёта. При двух правках ОЛ подряд запоминается самое раннее неразрешённое значение — инженер видит то, поверх чего ставил свою цифру.

**Узел — по ключу, а не по названию** (с 11.09.2026). В названиях узлов параметры ОЛ — «Нитка напорного трубопровода DN150 ×2», «Шахта обслуживания Ø1200 h2300», — и прежде при их смене правки узла пропадали молча, а выключенный узел включался снова. Теперь у узла есть ключ `slot` (`CalcComponent.slot`): материализация ставит `<узел шаблона>#<роль>`, у узла каталога — `cat:<код>#<n>` (`engines/template-def.ts`, materializeRef). Роль задаёт строитель, если набор его компонентов зависит от ОЛ: патрубки `inlet`/`outlet`, части напорного трубопровода, днища ёмкости `flat`/`elliptic`/`cylindrical`, люки; остальным хватает номера компонента. Что ключ во всех вариантах ОЛ указывает на узел одной роли, держит тест `engines/component-key.test.ts`. Узла с тем же ключом в свежем дереве нет — это узел другой роли (плоское днище сменилось эллиптическими), и его правки чужому узлу не достаются. Деревья, собранные до ключей, сопоставляются по названию, затем по его основе без параметров (`titleStem`, `engines/component-key.ts`).

**Строка со сменившимся наименованием.** Строка ищется по «вид + наименование + ЕИ». Наименование сменилось вместе с параметром ОЛ — DN муфты, длина штока задвижки, — и правки переходят на строку той же природы (вид, категория, ЕИ) с пометкой `renamedFrom`. Это конфликт: ячейка показывает «было «прежнее наименование»», «Оставить моё» сохраняет перенесённые правки, «Принять новое» сбрасывает количество, цену и выключение — они относились к прежней позиции.

**Что перенести некуда** — узла или строки той же природы в свежем дереве нет (подводящих патрубков стало ноль), — не пропадает молча: правка попадает в `CalcTree.lostEdits`, над таблицей расчёта — плашка «Не перенесено ручных правок: N» со списком. Правку можно вернуть строкой вручную в тот же раздел или убрать из списка; список хранится в дереве, пока инженер его не разберёт, а статус опросного листа сообщает, что он есть.

**Шаблон изменился между сборками** — технолог переставил разделы или перенёс узел в другой раздел. Раздел ищется по названию, номер — запасной ключ: номер раздела — его место в шаблоне и после перестановки меняется. Компонент — по ключу в своём разделе, а не нашёлся там — в любом: ручные правки переезжают вместе с узлом. Вставленные вручную компоненты (свободные строки и узлы каталога, `custom-…`) переносятся целиком; если их раздела больше нет — в последний раздел.

**Узлы, которые включает тумблер ОЛ** (теплоизоляция, фланцевый патрубок под задвижку, аварийный трубопровод, корзина, дробилка, шкаф управления, датчики давления и уровня, расходомер, у ёмкости и колодца — лестница и вентстояк), помнят ответ ОЛ в `enabledCalc` (`CalcComponent`, `engines/template-kns.ts`). Правило переноса — выигрывает последняя правка: сменился тумблер в ОЛ — узел берёт состояние из ОЛ; не менялся — остаётся, как его переключил инженер в расчёте (`reconciledEnabled`, `stores/calcTree.ts`). До 10.09.2026 состояние узла переносилось из старого дерева всегда, и выключенная в ОЛ теплоизоляция продолжала считаться. У деревьев, собранных раньше, прежний ответ ОЛ неизвестен — считается, что узел в расчёте не переключали.

### 8.4 ОЛ — основной экран изделия (с 2026-09-10)

Единица проекта открывается **опросным листом**, а не расчётом (`ProjectView.vue`, `openUnit`); в расчёт ведут «расчёт →» на карточке и «Открыть расчёт →» в ОЛ. Каждая правка ОЛ после паузы в вводе (`SYNC_DELAY_MS` = 700 мс) пересобирает и сохраняет расчёт одним запросом — ОЛ, дерево и итоги (`composables/useSurveySync.ts` → `applySurvey` стора). Кнопки «Сохранить ОЛ» больше нет; в шапке ОЛ — настоящий статус («сохранено 10:42 · расчёт пересчитан»), в живой панели — цена продажи.

- Нагрузка сверяется с сохранённой (`stableStringify` — jsonb хранит ключи в своём порядке): одинаковая — запроса нет. Без этого лист пересобирал расчёт при каждом открытии: марка насоса приходит от подбора асинхронно, и её появление выглядело правкой.
- Запросы не перекрываются: правка во время сохранения ставит следующее в очередь.
- «Создать расчёт» строит расчёт сразу и оставляет пользователя в ОЛ — уже как листе изделия (`/survey/:id`).
- Справочники и прайс кешируются на время работы с листом (`ensureContext`); экран расчёта при открытии берёт их свежими.
- Справочники или шаблоны изделий не загрузились — лист сохраняется **без расчёта**: статус «сохранено · шаблоны изделий не загрузились — расчёт пересоберётся позже». Ревизия ОЛ растёт, ревизия дерева — нет, и расчёт пересоберётся при следующей правке листа или открытии расчёта. Встроенный шаблон берётся, только если сервер ответил, что своих шаблонов нет: прежде сбой запроса `/api/refs/templates` молча собирал изделие встроенным шаблоном вместо действующего, и автосохранение записывало такое дерево (План_устранения, 1.4; `CalcDeferredError`, `stores/calc-errors.ts`).
- **Записи расчёта идут по очереди, чтение ждёт начатых** (`stores/calcTree.ts`, `enqueue`/`settled`). Уход с листа досохраняет последнюю правку, а экран расчёта в ту же секунду читает изделие; до 10.09.2026 чтение обгоняло запись — расчёт показывал прежнее дерево (цена, только что введённая в ОЛ, «не переносилась»), а первое его сохранение возвращало старую цену и в ОЛ. ОЛ так же ждёт незавершённого сохранения расчёта.
- **Устаревшая запись отклоняется сервером**: сохранение из расчёта с деревом, собранным из ревизии ОЛ старше сохранённой (расчёт открыт в другой вкладке, а ОЛ за это время поправили), получает `409 SURVEY_CHANGED` (`backend/src/utils/survey-write.ts`). Экран расчёта перечитывает изделие и просит повторить правку — дерево и цены ОЛ не откатываются.
- **Экран расчёта сохраняется сам** (с 11.09.2026, План_устранения 3.2, решение Р6): прежде правки жили в памяти до кнопки «Сохранить», и уход с экрана их молча терял. Теперь — как у ОЛ: через 1,5 с после последней правки запись, статус рядом с кнопкой («изменения…», «сохраняем…», «сохранено 21:40», «не сохранено: …»); «Сохранить» пишет сразу. Несохранённое — расхождение подписи текущего состояния (дерево, наценка, тираж) с подписью последней записи или загрузки (`savedSignature` в `stores/calcTree.ts`), поэтому пересчёт по прайсу, пересборка и выпуск КП, которые сохраняют сами, лишних записей не дают. Закрыть вкладку с несохранённым браузер не даст без вопроса; уход на другой экран сначала сохраняет, а не вышло — остаётся на месте один раз и объясняет. Дерево, пересобранное при открытии по изменившемуся ОЛ, тоже сохраняется само.
- **Запись поверх чужой правки не проходит** (с 11.09.2026, План_устранения 3.1): у расчёта есть версия (`Estimate.version`), каждая запись шлёт ту, с которой работала. Расчёт с тех пор сохранили в другой вкладке или другой пользователь — `409 ESTIMATE_CHANGED`. Опросный лист перечитывает расчёт и пересобирает дерево по своему ОЛ поверх чужих правок — ручные правки другой вкладки переносятся, как при любой пересборке, — и повторяет запись один раз, с ревизией ОЛ выше сохранённой: ревизия не уменьшается. Экран расчёта перечитывает изделие и просит повторить правку.

### 8.5 Цены, связанные с полями ОЛ

Цена трубы корпуса (₽/м.п., все три изделия), цена насоса (₽/шт, КНС) и цена трубы шахты ёмкости или горловины колодца (₽/м.п., связь `servicePipePrice` — диаметр у них свой, и цена корпуса к ним не подходит) вводятся в ОЛ и живут в расчёте ручной ценой своих строк. Строки помечены `priceBinding` (`engines/types.ts`); связь двусторонняя:

- ОЛ → расчёт: материализация кладёт поле в `priceManual`, при переносе правок цена связанной строки из старого дерева **не** переносится — ОЛ здесь источник;
- расчёт → ОЛ: правка такой цены в расчёте при сохранении пишется обратно в форму (`boundPricesPatch`), иначе следующая правка ОЛ вернула бы старую цифру.

Сегменты «трубы частями» — та же труба, связаны с тем же полем. Цены, введённые в расчёте до появления полей, при открытии ОЛ переезжают в пустые поля (`engines/price-binding.ts`, `boundPricesFromTree` — по пометке либо по приметам старых деревьев: труба шахты или горловины — строка трубы узла A8).

**Цены насосов — позиции прайса**: «Насосы, АТМ | Насос <марка> | шт», по одной на каждую из 61 модели каталога (`backend/prisma/seed.ts`, `seedPumpPrices`). Заводятся пустыми — цены у производителя в каталоге нет, их вносит закупка на экране прайса. Поле ОЛ «Цена насоса» перекрывает прайс; плейсхолдер показывает цену марки из прайса.

⚠️ **не реализовано:** массовое действие «сбросить все конфликты». В сторе есть только поштучные `keepOverride`/`dropOverride`, в UI — кнопки внутри одной строки (`ntt-calculator/src/components/calculator/CalcTableRow.vue:53-54`). Кнопка «сбросить ✕» в панели фильтров чистит фильтры, а не конфликты.

---

## 9. Механика итогов (экономика)

Пайплайн поверх суммы строк, единый для всех изделий:

```
часы_фитингов = Σ чел.ч работ без «изготов» в наименовании
часы_РМУ      = Σ чел.ч работ с «изготов»
масса_формовки = Σ qty строк с ЕИ=кг

ПЗР        = ROUNDUP(часы_фитингов / 11, 0.1) × ставка_ФОТ
Ацетон     = ROUNDUP(масса_формовки × 0.05, 0.1) × цена_ацетона
СИЗ и РМ   = ROUNDUP(часы_фитингов + 302) × цена_СИЗ       ← ЕИ «ед.», НЕ ПЗР (см. ниже)
Накладные  = ROUNDUP(часы_фитингов + ПЗР, 0.1) × ставка_накладных   ← 1584.73 ₽/чел.ч

Себестоимость (с НДС) = Материалы_закупка + Труба_муфта + Формовка
                      + Работы_ФОТ(вкл. ПЗР) + Прочие(Ацетон+СИЗ+Накладные)

Цена продажи   = ROUNDUP( Себестоимость × (1 + наценка), до 100 ₽ )   ← наценка 0.43
Рентабельность = (Цена − Себестоимость) / Цена                        ← справочно ≈ 30%
```

> **Уточнение по первоисточнику (2026-07-16).** Формулы сверены с `Шаблон 3.0.xlsx`, лист «Калькулятор КНС»:
> - ПЗР `J443 = ROUNDUP(часы/11; 0,1)` ✓, ацетон `J445 = ROUNDUP(масса×0,05; 0,1)` ✓, накладные `J447 = ROUNDUP(J441+J443; 0,1)` ✓ — совпадают.
> - **СИЗ `J446 = ROUNDUP(J441+302,)` — расходится с прежней редакцией.** ПЗР в формуле не участвует; `302` — константа шаблона, ЕИ «ед.». Реализуется как в эталоне; **❓ смысл константы 302 требует уточнения у завода**.
> - **ПЗР входит в корзину «Работы, ФОТ»** (`N453 = N441+N443+N442`), а **не** в «Прочие» (`N455 = ацетон+СИЗ+накладные`).
> - Себестоимость `N449 = N454+N451+N452+N455+N453` — ровно 5 корзин.

- Наценка — редактируемое поле расчёта (`ntt-calculator/src/views/CalculatorTreeView.vue:147-148`), сохраняется в `totals.markup` и поднимается при загрузке. Рентабельность считается тут же и видна в реальном времени. **По умолчанию — константа движка** `DEFAULT_MARKUP = 0.43` (`ntt-calculator/src/engines/economics.ts:59`): ⚠️ **не реализовано** взятие значения по умолчанию из настроек — подсистемы настроек в системе нет (см. §13).
- Ставки (ФОТ, накладные, ацетон, СИЗ) — позиции прайса, а не константы кода. С 11.09.2026 они **фиксируются в дереве** вместе с ценами строк (`CalcTree.rates`, План_устранения 1.3): материализация берёт их из того же прайса, что и цены строк (`ratesFromPrices`, `ntt-calculator/src/engines/economics.ts`), а пересчёт по прайсу обновляет их и показывает сдвиг каждой (`rateShifts`, `engines/reprice.ts`). Смена ставки в прайсе старый расчёт не меняет; ПЗР и ФОТ-спутники считаются по одной ставке; снапшот хранит дерево вместе со ставками, и экономика по нему восстанавливается. Дерево, собранное раньше, считается по действующему прайсу до первого сохранения — сохранение ставит ему ставки, по которым оно и посчитано. Ключи — `ФОТ|ФОТ|чел. ч`, `ФОТ|Накладные расходы|чел. ч`, `Прочие материалы|Ацетон|кг`, `Прочие материалы|СИЗ и РМ|ед.` (`RATE_ITEMS`). Позиции нет в прайсе — берётся константа программы (`FALLBACK_RATES`) с отметкой `rates.fallback`: экран расчёта предупреждает, а КП не выпускается (решение Р5, 422 `RATES_NOT_IN_PRICE`, §10).
- НДС отдельно не выделяется — цены прайса с НДС (правило предметной области). Печатная форма КП выделяет его «в том числе» справочной строкой, не начисляя сверху; ставка 22% — по образцу заказчика (`backend/src/utils/kp-document.ts`).

### 9.1 Тираж

Поле «Кол-во корпусов» — целое ≥ 1 (валидация `ntt-calculator/src/views/CalculatorTreeView.vue:500-505`); все количества строк домножаются на N (`ntt-calculator/src/engines/row.ts:44`).

Итоговый блок показывает **одну** колонку — за весь тираж. При N ≥ 2 к ней добавляется одна строка «за 1 корп.» с ценой продажи одного корпуса (`ntt-calculator/src/views/CalculatorTreeView.vue:167-169`): её нельзя получить делением итога на N, потому что ПЗР, СИЗ и округления нелинейны, — стор считает её отдельным прогоном экономики с `tirage = 1` (`economicsUnit`, `ntt-calculator/src/stores/calcTree.ts:350-358`).

⚠️ **не реализовано:** две колонки «за 1 корп.» / «за N корп.» со всеми составляющими. Себестоимость, корзины и рентабельность в пересчёте на один корпус в UI не выводятся, хотя `economicsUnit` вычисляет их целиком.

---

## 10. Прогрессия статусов и точка фиксации

> **Переписано 2026-07-16 по уточнению заказчика.** Прежняя редакция описывала
> согласование внутри калькулятора (CALC → REVIEW → APPROVED, «APPROVED
> замораживает расчёт»). Это **не соответствует реальному процессу**.

Реальный процесс:

```
Предварительный расчёт → КП → согласование КП в коммерческом отделе
   │                                    (вне калькулятора)
   │                                          │
   ▼                                          ▼
снапшот при выпуске КП            дальнейшая проработка инженером:
(что именно ушло заказчику)       трубы, оборудование, насосы, детали
                                            │
                                            ▼
                        сметы, спецификации, списки для ERP, закупка
                                     (не проработано)
```

**Расчёт не замораживается.** Инженер правит его *после* выпуска КП — это
следующий шаг процесса, а не нарушение. Требование «APPROVED замораживает
расчёт, редактирование блокируется» отменено.

**Снапшот привязан к выпуску КП, а не к статусу.** Он фиксирует, из каких цен
и какой версии прайса родилась цифра в согласуемом документе: прайс меняется,
и без снапшота подписанную цифру через месяц не восстановить. Расчёт при этом
живёт дальше — снапшоты и есть механизм версионирования (§4).

**Версия шаблона снапшота** — та, по которой собран состав дерева
(`tree.templateVersion`, `treeTemplateVersion` в
`backend/src/utils/estimate-tree.ts`): 0 — встроенный шаблон, N —
опубликованная технологом версия, пусто — слепок снят с дерева без отметки.
Рядом — **редакция встроенного шаблона** (`tree.builtinRevision`,
`treeBuiltinRevision`): версия кода, которым собран состав; пусто — дерево
собрано до учёта редакций. Окно «Версии» показывает обе рядом с версией
прайса («НН v2 · шаблон v1 · ред. 3»): по ним видно, каким составом, каким
кодом и по каким ценам посчитано выпущенное КП — и после релиза, поменявшего
формулы узлов.

**Версия прайса снапшота — та, по которой посчитаны цены строк**
(`tree.priceListVersion`, `treePriceListVersion` в
`backend/src/utils/estimate-tree.ts`), а не действующая: расчёт, собранный по
v2 и не пересчитанный после импорта v5, раньше уходил в КП с подписью «прайс
v5» при ценах v2. Если действующий прайс новее, «Сформировать КП» сначала
спрашивает: пересчитать цены и выпустить, выпустить по прежней версии или
отменить (§5.2).

**У снапшота есть причина** (`EstimateSnapshot.reason`, `backend/src/utils/snapshot-reason.ts`):

| Причина | Когда снимается | КП печатается | Мешает удалить единицу |
|---|---|---|---|
| `CREATE` | при создании единицы изделия — временной слепок исходного состояния | нет | нет |
| `MANUAL` | ручная фиксация в окне «Версии» | да | да |
| `KP` | выпуск КП, после проверки строк без цены | да | да |

Слепок создания нужен потому, что ОЛ пересобирает расчёт при каждой правке
(§8.4): без него то, с чего единица начала, не восстановить. Он снимается один
раз — после первой сборки расчёта, повтор возвращает уже снятый. КП по нему не
печатается (422 `KP_NOT_PRINTABLE`): проверку строк без цены он не проходил, и
документ занизил бы итог «красными» строками. По той же причине он не делает
единицу «готовой» для КП на проект и не мешает её удалить — иначе
неудаляемой стала бы каждая новая единица. Снапшоты, снятые до появления
причины, получили `KP` — так они и работали.

**Красные строки (нет цены) блокируют выпуск КП.** Их сумма равна нулю, то
есть такая строка молча занижает итог; в документ заказчику это попасть не
должно. Типовой случай — труба корпуса: цена договорная (§5.2). Гейт стоит в
`POST /api/estimates/:id/kp` — 422 `ROWS_WITHOUT_PRICE`
(`backend/src/routes/estimates.routes.ts`), там же снимается снапшот. Тот же
маршрут не выпускает КП со строками с отрицательной суммой (422
`NEGATIVE_ROWS`, решение Р4, §5.1) и по ставкам экономики, взятым
константами программы (422 `RATES_NOT_IN_PRICE`, решение Р5, §9).

**Печатная форма КП строится из снапшота**, а не из текущего состояния:
`GET /api/estimates/:id/kp/export?format=docx|pdf[&version=N]`
(`backend/src/routes/estimates.routes.ts:355`). Без снапшотов — 422
`KP_NOT_ISSUED`: печатать нечего. Кнопки `docx`/`pdf` живут в окне «Версии»
калькулятора (`ntt-calculator/src/views/CalculatorTreeView.vue:242-251`),
каждая выгрузка пишется в аудит как `estimate.kp.export`. Дата документа —
дата снапшота **по Москве** (решение Р10, `formatDate` в
`backend/src/utils/kp-document.ts`): контейнер живёт по UTC, и КП,
выпущенное до 03:00 по Москве, прежде получало вчерашнюю дату.

**КП выпускается на единицу и на проект целиком.**
`GET /api/projects/:id/kp/export?format=docx|pdf[&estimates=id,id]`
(`backend/src/routes/projects.routes.ts`) собирает документ из последних
снапшотов единиц проекта: по позиции на единицу, у каждой своя спецификация,
своя редакция и своя цена, общая сумма — их сложение. Единица без снапшота
не выбрасывается молча, а даёт 422 `KP_UNITS_NOT_ISSUED` с её именем;
`estimates` ограничивает состав, если КП нужно на часть проекта. Модель
документа одна на оба случая (`utils/kp-document.ts`, `KpPosition`):
КП на единицу — документ из одной позиции. Аудит — `project.kp.export`.

**Работы и ФОТ в КП не печатаются** (`utils/estimate-tree.ts`, `isWorkRow`):
заказчик покупает изделие, а не трудозатраты. Операции собственного
производства («Ламинирование частей корпуса 112 кг», «Прорезка отверстия
0,7 чел. ч») и их спутники ФОТ занимали три четверти строк спецификации: в КНС
из 58 строк осталось 23. На цену это не влияет — она берётся из снапшота, куда
работы входят через себестоимость. Из гейта «строки без цены» работы не
исключаются: там они занижают сумму по-настоящему.

⚠️ Следствие: раздел, в котором нет ни одной закупаемой позиции, из документа
пропадает целиком — так исчезла «Лестница» (в ней только изготовление и
монтаж). Это ограничение построчной спецификации; лечится переходом на
узловую, где позиция документа — узел изделия («Лестница, 1 компл.»), как в
образце заказчика. См. `Вопросы_заводу.md` §8.

**Количества в КП — на весь тираж**, как и цена продажи: состав и сумма в
одном документе обязаны относиться к одному заказу, поэтому число изделий
печатается в подписи позиции.

**Количество, заданное выражением, считает фронт.** `qtyManual` хранит
выражение (§5.1), парсер живёт только в `engines/expr.ts`; заводить второй на
сервере — значит рассинхронизировать грамматику. Поэтому при сохранении в
каждую строку дерева кладётся `qtyResolved` — результат за одно изделие
(`ntt-calculator/src/stores/calcTree.ts`, `treeForSave`), и бэкенд читает его,
а не разбирает выражение (`backend/src/utils/estimate-tree.ts`,
`resolveRowQty`). Если результата нет (снапшот снят до появления поля),
количество считается НЕОПРЕДЕЛЁННЫМ, а не нулевым: строка без цены блокирует
выпуск КП, а печать отказывает с 422 `KP_SPEC_INCOMPLETE` — выдуманное число в
документе заказчику хуже отказа.

**Статусы в Фазе 1:** используются `DRAFT` и `CALC`; `DRAFT → CALC` происходит
при первом сохранении расчёта. Переход в `REVIEW`/`APPROVED` **запрещён** —
`PATCH /api/estimates/:id/status` отвечает 422 `STATUS_FLOW_REMOVED`
(`backend/src/routes/estimates.routes.ts:125-131`); единственные разрешённые
переходы — `DRAFT → CALC` (ENGINEER/MANAGER/ADMIN) и `→ REJECTED` (ADMIN).
`REVIEW`/`APPROVED` остаются в enum БД (`backend/prisma/schema.prisma:26-32`)
ради старых расчётов, и их русские подписи рендерятся в UI, чтобы такой
расчёт не выглядел сломанным (`ntt-calculator/src/views/CalculatorTreeView.vue:371`,
`ntt-calculator/src/views/ProjectView.vue:252`). От legacy-`APPROVED` осталась
заморозка: правка ОЛ такого расчёта отдаёт 422 `ESTIMATE_FROZEN`
(`backend/src/routes/estimates.routes.ts:505-512`), а удаление запрещено.

---

## 11. Экраны

Маршруты — `ntt-calculator/src/router/index.ts:22-95`.

| Экран | Механики |
|---|---|
| Опросный лист | Единая анкета с ветвлением по типу изделия (`SurveyView` → `SurveyKnsView`/`SurveyEmkView`/`SurveyKolView`); live-подсказки (цепочка подбора глубины Q · Vэф · Vмин · hраб · hР и авторасчёт Нподз — `ntt-calculator/src/views/SurveyKnsView.vue:372-381`, `:222`; марка трубы); кнопка «Создать расчёт» |
| Калькулятор | Дерево разделов со свёрнутыми подытогами; строки с индикацией источника (серый=расчёт, синий=override, красный=нет цены, выключенные — под чекбоксом «выключенные», §7.2); панель итога закреплена; счётчик проблем; окно «Версии» с выгрузкой КП в docx/pdf. VIEWER открывает экран только на просмотр |
| Прайс | Каталог НН с категориями и поиском; правка цены и поставщика; импорт xlsx. Правка цены и импорт создают новую версию прайса; действующая — в шапке экрана, чем она создана — в сноске. ⚠️ **не реализовано:** список версий и diff версий — эндпойнтов перечисления и сравнения версий нет |
| Заявка на закупку | Read-only view закупочных строк; экспорт xlsx (`POST /api/estimates/:id/purchase-request/export`, `backend/src/routes/purchase.routes.ts:52-53`): строки считает движок на фронте и шлёт в теле запроса, бэкенд только форматирует книгу (`ntt-calculator/src/views/PurchaseRequestView.vue:130-134`) |
| Шаблоны | Правка справочников материализации технологом: нормы патрубков, веса труб, инженерные матрицы; каждая правка — в AuditLog (`backend/src/routes/templates.routes.ts`) |
| ⚠️ **не реализовано:** Гидравлика | Экрана нет: ни `HydraulicsView.vue` в `ntt-calculator/src/views/`, ни маршрута, ни графической библиотеки в зависимостях. **Расчётная часть на бэкенде есть** — `POST /api/pump-station/dimensions` (габарит станции), `/ring-stiffness` (SN по глубине патрубка и ТТ МВК), `/discharge-pipe-diameter` (диаметр напорного из расхода, `D = √(4Q/πV)`, V по умолчанию 1,5 м/с), `/select-pump` (подбор по рабочей точке из каталога `Pump`, 61 модель Vandjord VSL) — `backend/src/routes/pump-station.routes.ts`. Подбор насоса и диаметра напорного ПОДКЛЮЧЕН к опросному листу КНС (`composables/usePumpSelection.ts`, блоки «Насосное оборудование» и «Патрубки»). Подбор идёт по паспортной кривой насоса (`PumpCurvePoint`, 559 точек у 56 моделей из 61). Нет: отдельного экрана, кривой СИСТЕМЫ и графика их пересечения — геометрический напор и потери в сети в ОЛ не задаются |

---

## 12. Инварианты и антиправила

1. Сумма итогового блока всегда равна сумме включённых строк + начисления §9 (в Excel это контролировалось дублирующей формулой — здесь инвариант движка, покрывается тестами).
2. Ручной ввод никогда не перезаписывается системой без подтверждения. Правка, которую пересборка перенесла на строку с другим наименованием, ждёт подтверждения (`renamedFrom`); правка, которую перенести некуда, остаётся в списке `lostEdits` (§8.3).
3. Формулы живут в реестре функций (`ntt-calculator/src/engines/formulas.ts`), состав узлов — в шаблоне. Вставка узла в расчёт — **материализация**: копия состава с зафиксированными расчётными значениями, применёнными ценами и коэффициентами. Дерево самодостаточно **по строкам**: изменения шаблона и каталожных цен влияют только на новые вставки. Опубликованная новая версия шаблона изделия готовый расчёт не меняет: экран расчёта показывает, по какой версии собран состав и какая действует, и предлагает пересобрать (§7.3); пересборка по правке ОЛ тоже берёт действующий шаблон. Четыре ставки экономики (§9) с 11.09.2026 тоже хранятся в дереве: смена ставки в прайсе старый расчёт не меняет, пока его не пересчитают по прайсу. Точка фиксации документа — **снапшот при выпуске КП**, а не статус (§10): снапшот создаётся в `POST /:id/kp` и вручную в `POST /:id/snapshot`; обработчик смены статуса снапшотов не снимает, а переход в `APPROVED` запрещён.
4. Ключ прайса — тройка (категория, наименование, ЕИ); символ `~` в наименованиях запрещён (наследие VLOOKUP). Валидация стоит на импорте прайса: строка с `~` пропускается с причиной (`backend/src/utils/nn-sheet.ts:103-106`).
5. Удаление строки шаблона запрещено — только выключение; удалять можно только строки, добавленные вручную (`removeRow` ищет строку с флагом `isCustom`, `ntt-calculator/src/stores/calcTree.ts:476-484`).
6. Балансные константы (см. §13) не хардкодятся по строкам, а задаются один раз — именованными экспортами в `formulas.ts`, `fot.ts`, `economics.ts`, `rounding.ts`.

---

## 13. Таблица баланса (все числовые константы механики)

Все пути ниже — от корня репозитория. Константы движка — именованные экспорты
модулей `engines/*`; меняются релизом.

| Константа | Значение | Где живёт | Источник |
|---|---|---|---|
| Плотность ламината `LAMINATE_DENSITY` | 1850 кг/м³ | `ntt-calculator/src/engines/formulas.ts:16` | геометрия масс |
| Ламинирование детали к корпусу `LAMINATION_SHARE` | 3/10 массы детали | `ntt-calculator/src/engines/formulas.ts:19` | норматив |
| k ФОТ: мех. формовка / ламинирование / ручная | 0.28 / 0.56 / 1.0 чел.ч/кг | `ntt-calculator/src/engines/fot.ts:15-17` | §6 |
| Товарный вид трубы `MARKETABLE_DIVISOR` | DN/1300 чел.ч на м | `ntt-calculator/src/engines/formulas.ts:22` | норматив |
| Прорезка отверстия `CUTOUT_HOURS_PER_M` | 0.5 чел.ч на 1 м окружности | `ntt-calculator/src/engines/formulas.ts:25` | норматив |
| Подготовка трубы `PIPE_PREP_DIVISOR` | DN/(200·6) чел.ч на м | `ntt-calculator/src/engines/formulas.ts:28` | норматив |
| Гильза патрубка `sleeveDiameter` | DN+100, CEILING до 50 (<200) / 100 (≥200); при DN < 100 надбавка +100 **не** применяется | `ntt-calculator/src/engines/survey-kns.ts:233-237` | геометрия (эталон `K8`/`M8`) |
| Несущая способность анкера `ANCHOR_CAPACITY_N` | 27 500 Н (анкер М16); в той же формуле ρ воды 1000 кг/м³ и g = 9,8 м/с² | `ntt-calculator/src/engines/formulas.ts:31,34,37` | расчёт всплытия |
| Лестница | изготовление 1,25·H чел.ч, монтаж = изготовление/2; тетивы — уголок H·2 м; ступени — труба H·0,44/0,35 м (`LADDER_RUNG_WIDTH_M`, `LADDER_RUNG_STEP_M`) | `ntt-calculator/src/engines/formulas.ts`, `ladder` | эталон, строки 124–125 и 129–130 |
| Направляющие насосов `pumpGuidesM` | труба L·(раб+рез)·2 м; работы по 2 чел.ч на насос на изготовление и на монтаж | `ntt-calculator/src/engines/formulas.ts`, `ntt-calculator/src/engines/station-equipment.ts` | эталон, строки 190 и 245–246 |
| Рама насосов и рама перекрытия `frameHours` | изготовление 6 / монтаж 5 чел.ч при DN < 2500, иначе 7 / 6 | `ntt-calculator/src/engines/formulas.ts` | эталон, строки 218–219 и 247–248 |
| Цепь подъёма насосов `pumpLiftChainM` | ROUNDUP((раб+рез)·(H + 1)) м, H — высота станции | `ntt-calculator/src/engines/formulas.ts` | эталон, строка 195 |
| Монтажные петли | 4 шт; от DN 2000 усиленные; работы ROUNDUP(0,5 × петель) | `ntt-calculator/src/engines/mounting-loops.ts` | «Вспомогат. таблицы», эталон, строки 63–72 и 90–91 |
| Утепление горловин люков КНС `KNS_NECK_INSULATION_M2` | ((1,5+1,5)·2 + π·0,65)·0,94 ≈ 7,56 м² | `ntt-calculator/src/engines/formulas.ts` | эталон, J58 |
| Поплавковые выключатели `floatSwitchCount` | раб + рез + 2 шт | `ntt-calculator/src/engines/survey-kns.ts:266-268` | норматив |
| Ступени жёсткости `SN_BASE` и порог глубины | 5000 / 10000; порог 7000 мм (выше — 10000). Флаг «под проезжей частью» поднимает на ступень | `ntt-calculator/src/engines/survey-kns.ts` (`snByDepth`), то же правило на сервере — `backend/src/utils/ring-stiffness.ts`; общие примеры — `backend/src/utils/sn-rule.vectors.json` | завод (единственные две ступени производства) |
| Обозначения жёсткости по ТТ МВК | 5000 → 8000, 10000 → 12000 (только в марке; масса и трудоёмкость по расчётной) | `ntt-calculator/src/engines/survey-kns.ts:161` | ТТ МВК |
| PN опросного листа `PN_SURVEY_DEFAULT` | 0.1 (корпус безнапорный); в ключ поиска веса идёт PN трубы из `pnForWeightLookup` | `ntt-calculator/src/engines/survey-kns.ts:127,196` | эталон `G8`/`F7` |
| Толщина защитного слоя ламинации на ТИ | 5 мм у КНС, 4 мм у ЕМК и КОЛ — входит в наименование позиции прайса | `ntt-calculator/src/engines/template-kns.ts`, `ntt-calculator/src/engines/template-emk-kol.ts` (`INSULATION_LAYER_MM`) | листы ЕМК и колодца (0,004·1850) |
| Толщина верхнего перекрытия | 6 мм у КНС, 10 мм у ЕМК и КОЛ; минус крышки люков у ЕМК и КОЛ | `ntt-calculator/src/engines/formulas.ts` (`topSlabMassKg`), `buildSlab` | листы: КНС строка 141, ЕМК 133, колодец 132 |
| Гильза патрубка | отрезок трубы `СК/НПС-К Ø-0,1-2500` 0,5 м на патрубок от DN 200 (`SLEEVE_PIPE_M`, `SLEEVE_PIPE_FROM_DN`); меньше — ручная формовка 0,5 кг; до DN 300 включительно (`FORMED_SLEEVE_MAX_DN`) у напорных КНС и стеклопластиковых патрубков ЕМК — «Формовка гильз» Мф × кол-во; стеклопластиковый патрубок КОЛ и КНС — «Муфта-2» вместо гильзы; ламинирование Мф·3/10 с k ФОТ 1 | `ntt-calculator/src/engines/template-kns.ts` (`buildSleeveNozzles`) | листы КНС 26–30 и 40–44, ЕМК 36–46, колодец 25–34 |
| Фланцевый патрубок КНС | отрезок трубы Ø гильзы 0,3 м на патрубок от DN 300 (`FLANGE_NOZZLE_PIPE_M`, `FLANGE_NOZZLE_PIPE_FROM_DN`); меньше — ручная формовка π·DN·0,005·0,3·1850 кг; ламинирование Мф(DN)·3/10 с k ФОТ 1 | `ntt-calculator/src/engines/template-kns.ts` (`buildKnsInletFlange`) | лист КНС, строки 32–38 |
| Марка трубы шахты и горловины | шахта SN 5000, горловина SN 2500, PN 0,1 (`SHAFT_PIPE_SN`, `NECK_PIPE_SN`) | `ntt-calculator/src/engines/template-emk-kol.ts` | вписано в листах: ЕМК H54/J54, колодец H36/J36 |
| Транспортный отрезок трубы ёмкости `TRANSPORT_PIPE_M` | 6 м: муфт отрезков ROUNDDOWN((L − 1,5)/6) у горизонтальной | `ntt-calculator/src/engines/template-emk-kol.ts` | лист ЕМК, H22 и строка 23 |
| Крепление горизонтальной ёмкости | ремень на метр трубы (вверх до целого), 2 анкера SZ-B на ремень, 2 рым-гайки на анкер, монтаж 0,5 чел.ч на ремень | `ntt-calculator/src/engines/template-emk-kol.ts` (`buildEmkStrapping`) | лист ЕМК, строки 174–176 и 201 |
| ПЗР `PZR_DIVISOR` | часы_фитингов / 11 | `ntt-calculator/src/engines/economics.ts:53` | экономика (эталон `J443`) |
| Ацетон `ACETONE_MASS_SHARE` | 5% массы формовки | `ntt-calculator/src/engines/economics.ts:56` | экономика (эталон `J445`) |
| СИЗ: базовая надбавка `SIZ_BASE_UNITS` | 302 ед. | `ntt-calculator/src/engines/economics.ts:50` | эталон `J446` (❓ смысл требует уточнения у завода) |
| Ставка ФОТ | 1207.8 ₽/чел.ч | позиция прайса `ФОТ\|ФОТ\|чел. ч`; fallback `ntt-calculator/src/stores/calcTree.ts:39` | НН |
| Ставка накладных | 1584.73 ₽/чел.ч | позиция прайса `ФОТ\|Накладные расходы\|чел. ч`; fallback `…calcTree.ts:40` | НН |
| Цена ацетона | 109.4 ₽/кг | позиция прайса `Прочие материалы\|Ацетон\|кг`; fallback `…calcTree.ts:41` | НН |
| Цена СИЗ и РМ | 122 ₽/ед. | позиция прайса `Прочие материалы\|СИЗ и РМ\|ед.`; fallback `…calcTree.ts:42` | НН |
| Наценка по умолчанию `DEFAULT_MARKUP` | 0.43 | `ntt-calculator/src/engines/economics.ts:59` | экономика (константа во всех 6 реальных файлах) |
| Округление цены продажи `roundUpPrice` | до 100 ₽ вверх (`ROUNDUP(…; −2)`) | `ntt-calculator/src/engines/rounding.ts:57` | экономика |
| Округление чел.ч `roundUpHours` | ROUNDUP до 0.1 | `ntt-calculator/src/engines/rounding.ts:54` | движок |
| Целевая рентабельность | ≈ 30% (справочно, не константа кода — производная от наценки) | — | экономика |
| Ставка НДС для строки «в том числе» в КП | 22% (образец «КПВ6393»: «цены с учётом НДС 22%») | `backend/src/utils/kp-document.ts` | печатная форма |

Ставки ФОТ, накладных, ацетона и СИЗ действительно меняются через прайс, и
правка позиции пишется в AuditLog (`prices.update`, `backend/src/routes/prices.routes.ts:63`);
импорт новой версии — `prices.import` (там же :197). Правки справочников
материализации технологом тоже пишутся в аудит (`template.*`,
`backend/src/routes/templates.routes.ts`).

⚠️ **не реализовано:** «изменение любой константы — через настройки, с записью
в AuditLog». Все остальные константы таблицы заданы литералами в TypeScript и
меняются только релизом: модели `Setting` в `backend/prisma/schema.prisma` нет,
экрана и эндпойнта настроек тоже. Действия `AuditLog` перечислены явным типом
`AuditAction` (`backend/src/utils/audit.ts:16-34`), и события «изменена
константа» среди них нет.
