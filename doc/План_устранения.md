# План устранения проблем

**Дата:** 11.09.2026
**Основание:** аудит кода и документации ветки `dev` (коммит `61517b6`), 11.09.2026
**Статус:** к исполнению. Сделанное отмечается в сводке и у пункта: «✅ дата, коммит».

Пять этапов — от того, что может остановить сервер или молча изменить цену
расчёта, к документации. Каждый пункт — отдельный коммит в `dev` вместе с
тестами и правкой документации. Размер: S — до двух часов, M — полдня–день,
L — два-три дня; оценки грубые.

Порядок: этап 0 целиком, затем 1.1. Пункт 4.1 от остальных не зависит и
делается сразу.

Отчёт аудита в репозиторий не выносится. По безопасности здесь только задачи,
без описания текущего состояния: репозиторий публичный.

## Сводка

| Пункт | Что | Размер | Статус |
|---|---|---|---|
| 0.1 | Сид не останавливает запуск и не возвращает удалённое | M | ✅ 11.09.2026, `b08796b` |
| 0.2 | Сид не печатает пароли | S | ✅ 11.09.2026, `d6c3799` |
| 0.3 | Деплой проверяет, что сервер поднялся | S | ✅ 11.09.2026, `a191e9e` |
| 0.4 | Дампы: секреты через окружение, выгрузка устойчива к ошибкам | S | ✅ 11.09.2026, `c0e06cf` |
| 0.5 | Экран «Сменить пароль» | S | ✅ 11.09.2026, `4568434` |
| 1.1 | Перенос правок по стабильному ключу, потери видны | L | ✅ 11.09.2026, `061da2a` |
| 1.2 | Ручная правка цены поднимает версию прайса | M | — |
| 1.3 | Ставки экономики фиксируются в дереве | M | — |
| 1.4 | Сбой загрузки шаблонов не подменяет шаблон | S | — |
| 1.5 | Отрицательные и неразобранные значения | M | — |
| 1.6 | Дата и номер КП | S | — |
| 2.1 | Первый администратор, обязательная смена, сброс пароля | M–L | — |
| 2.2 | Ограничение частоты входа | S–M | — |
| 2.3 | Отзыв токенов | S | — |
| 2.4 | Заголовки безопасности, шрифт в сборке | S–M | — |
| 2.5 | Пароли, 409 на повтор email, аудит удаления, демо-вход | S | — |
| 2.6 | По желанию: меньше прав у БД и контейнера | M | — |
| 3.1 | Защита от записи поверх чужой правки | M | — |
| 3.2 | Экран расчёта не теряет несохранённое | M | — |
| 3.3 | Восстановление дампа не ломает запуск | M | — |
| 3.4 | Ротация дампов по видам | S | — |
| 3.5 | По желанию: сервер сверяет итог КП | L | — |
| 3.6 | Смена справочников видна в расчёте | M | — |
| 3.7 | Роль BUYER | M | — |
| 3.8 | Мелочи | S | — |
| 4.1 | Расхождения документации с кодом | M | ✅ 11.09.2026 |
| 4.2 | Ссылки на код — по имени, проверка в CI | S | — |
| 4.3 | Устаревшие комментарии в коде | S | — |

## Как работаем

- Перед коммитом: во фронтенде `npm test`, `npm run type-check`,
  `npm run check:lint`; в бэкенде `npm test` и `npm run build`. После пуша —
  зелёный CI.
- Проверка на живом стеке — на одноразовых единицах (заказчик «Тестовый
  заказчик», название «… (удалить)») и на временной базе рядом с рабочей;
  после проверки всё удаляется.
- Изменился состав встроенного шаблона — новая редакция в
  `engines/builtin-revisions.ts`. Служебные поля в отпечаток
  (`engines/builtin-fingerprint.ts`) не входят.
- Документация правится в том же коммите, что и поведение.
- Закупочные цены и `doc/*.xlsx` не публикуются.

## Этап 0. Срочно: запуск сервера и пароль

### 0.1 Сид не останавливает запуск и не возвращает удалённое — ✅ 11.09.2026, `b08796b`

**Проблема.** Сид выполняется при каждом старте контейнера
(`backend/docker-entrypoint.sh`) и требует, чтобы число строк справочников в
базе точно совпадало с JSON (`backend/prisma/seed.ts`: `seedPipeWeights`,
`seedEngineering`, `seedJointLayers`, `verify`). Экран «Шаблоны» добавляет и
удаляет строки этих справочников. Воспроизведено на временной базе:

- добавлена норма патрубка DN 1100 — сид завершается ошибкой «нормы патрубков:
  в JSON 26, в БД 27», контейнер уходит в цикл перезапуска;
- удалена норма DN 150 — при следующем старте сид её вернул;
- Мс (DN 3000, PN 4) изменена со 112 на 113 — сид падает на контрольной
  проверке.

**Что сделать.**

- Справочники (веса GRP- и ПЭ-труб, инженерные матрицы, нормы патрубков, Мс)
  заливать только в пустую таблицу. JSON — стартовое состояние, дальше
  справочники ведёт технолог (решение Р1).
- Прайс, насосы, их кривые и цены насосов по-прежнему досоздавать: удалить их
  из интерфейса нельзя.
- Сверки количеств и контрольных значений — только при `SEED_STRICT=1` (CI,
  пустая база). При старте контейнера расхождение — предупреждение в логе.
- Новые справочные данные будущих релизов доставлять миграцией.

**Проверка.** Три сценария выше на временной базе: сервер поднимается, правки
технолога целы. В CI — шаг «повторный сид на изменённой базе».

### 0.2 Сид не печатает пароли — ✅ 11.09.2026, `d6c3799`

Сообщение о первом администраторе — только в момент его создания и без
пароля.

### 0.3 Деплой проверяет, что сервер поднялся — ✅ 11.09.2026, `a191e9e`

После `docker compose up` задача деплоя (`.github/workflows/ci-cd.yml`) до
трёх минут ждёт ответа `/api/health`. Не дождалась — выводит хвост логов
бэкенда и завершается ошибкой.

### 0.4 Дампы: секреты через окружение, выгрузка устойчива к ошибкам — ✅ 11.09.2026, `c0e06cf`

- `pg_dump` и `pg_restore` получают параметры подключения через переменные
  `PG*`, а не аргументом командной строки; тексты ошибок проходят через
  маскировку строки подключения (`backend/src/utils/db-dump.ts`).
- Выгрузка дампа (`GET /api/admin/backups/:name`) — через `pipeline` с
  обработкой ошибки чтения.
- Тест на маскировку.

Скрипты `backend/scripts/` по-прежнему передают строку подключения аргументом:
они работают в терминале оператора, и их вывод в ответы API не попадает.

### 0.5 Экран «Сменить пароль» — ✅ 11.09.2026, `4568434`

Форма «текущий / новый / повтор» в меню пользователя поверх готового
`POST /api/auth/password`. После выкладки — сменить пароль администратора на
всех экземплярах.

Сделано общим блоком пользователя (`components/ui/UserMenu.vue`): «Сменить
пароль» и «Выйти» на экранах проектов, проекта, прайса, администрирования и в
топбаре «Шаблонов». Попутно закрыто: «Выйти» прежде было только на экране
проектов, куда технолога и снабженца роутер не пускает, — выйти им было
неоткуда.

## Этап 1. Цена расчёта

### 1.1 Перенос правок по стабильному ключу, потери видны — ✅ 11.09.2026, `061da2a`

**Проблема.** При пересборке дерева (`reconcileTrees` в `stores/calcTree.ts`)
компонент ищется по точному названию, а в названиях — параметры ОЛ: «Нитка
напорного трубопровода DN150 ×2», «Патрубок напорный DN150 ×2», «Шахта
обслуживания Ø… h…», «Горловина Ø… h…», монтажные петли «×N». Строки ищутся по
наименованию, в нём тоже DN и длина штока. Сменился параметр — ручные
количества, цены и выключения узла пропадают без предупреждения, выключенный
узел включается снова. Воспроизведено тестом: выключенная нитка напорного и
ручные количества 99 и 77 после смены числа напорных 2 → 3 — нитка включена,
количества сброшены, конфликтов 0. Это нарушает правило Механики §8.3 и §12.2.

**Что сделать.**

- В `materializeRef` (`engines/template-def.ts`) давать компоненту `slot`, не
  зависящий от параметров ОЛ: `<ref>#<роль>` у встроенного узла
  (`kns.nozzles#inlet`), `cat:<код>#<n>` у узла каталога. Роль явно задают
  строители с переменным набором компонентов: патрубки, напорный
  трубопровод, днища ЕМК.
- `reconcileTrees`: сначала по `slot`; у старых деревьев без `slot` — по
  названию, затем по названию без параметров (DN, ×N, Ø, h).
- Строки: «вид + наименование + ЕИ», иначе «вид + категория + ЕИ + порядковый
  номер» внутри узла. Такой перенос помечается «наименование изменилось: было
  «…»» — это конфликт, который подтверждает инженер. Ручная цена на строку со
  сменившимся наименованием переносится только с этой пометкой: другой DN —
  другая позиция.
- Всё, что перенести не удалось, — в `tree.lostEdits` и плашку «N ручных
  правок не перенесено» со списком и действиями «вернуть строкой вручную» /
  «убрать». Тост «ручные правки перенесены» — только без потерь.
- `slot` в отпечаток не входит — новая редакция не нужна.

**Тесты.** Стор (`stores/calcTree.test.ts`, «перенос правок при смене
параметров ОЛ»): сменилось число напорных — выключенный узел и ручные
количества переносятся; сменился DN подводящего из стеклокомпозита — правка
переходит на строку с новым наименованием конфликтом, «Оставить моё» и
«Принять новое»; узла больше нет — правка в `lostEdits`, сохраняется с
деревом и возвращается строкой; список переживает следующую пересборку;
старое дерево без `slot` — по основе названия. Перенос узла технологом в
другой раздел держит прежний тест шаблонов. Ключи
(`engines/component-key.test.ts`): на всех вариантах ОЛ отпечатка у каждого
узла КНС, ЕМК и КОЛ ключ есть, он один в дереве и указывает на узел одной
роли — так покрыты шахта ЕМК, горловина КОЛ и петли; отдельных тестов стора
на них нет.

**CI.** Прогон `061da2a` упал: моки стора в тестах опросных листов не знали
`lostEdits`, vitest поймал необработанные ошибки и вышел с кодом 1; деплой
пропущен. Исправлено в `1e5a66e`. Проверки перед коммитом смотрят код выхода,
а не хвост вывода.

### 1.2 Ручная правка цены поднимает версию прайса

**Проблема.** Версия прайса растёт только при импорте. После правки цены на
экране прайса (`PATCH /api/prices/:id`) плашка «прайс обновился» и вопрос при
выпуске КП не срабатывают; два расчёта с подписью «НН v5» могут стоять на
разных ценах.

**Что сделать.** Правка цены создаёт новую `PriceListVersion` («Ручная
правка: <позиция>») в одной транзакции с `PriceHistory` (решение Р3). Плашку
«цены устарели» показывать по фактическому расхождению цен строк и ставок с
действующим прайсом (`repricePreview`), а не только по номеру версии.

**Проверка.** Правка цены под одноразовым расчётом: плашка появляется, выпуск
КП предлагает пересчитать.

### 1.3 Ставки экономики фиксируются в дереве

**Проблема.** Ставки ФОТ, накладных, ацетона и СИЗ читаются из текущего
прайса при каждом открытии (`utils/materialize-context.ts`). После смены
ставки ФОТ-спутники считаются по старой ставке, а ПЗР — уже по новой;
экономику по снапшоту не восстановить; пропавшая из прайса ставка молча
заменяется константой `FALLBACK_RATES`.

**Что сделать.**

- `CalcTree.rates` с источником каждой ставки (прайс или константа) — при
  сборке и пересчёте по прайсу; экономика берёт ставки дерева. Старые деревья
  без `rates` считаются как сейчас, до первого пересчёта.
- Пересчёт по прайсу обновляет и ставки; сводка показывает сдвиг всех четырёх.
- Ставка не найдена в прайсе — предупреждение на экране, выпуск КП — по
  решению Р5.

**Тесты.** Смена ставки не меняет открытый старый расчёт; пересчёт меняет итог
и показывает сдвиг; ФОТ и ПЗР — по одной ставке.

### 1.4 Сбой загрузки шаблонов не подменяет шаблон

Сейчас ошибка запроса `/api/refs/templates` превращается во встроенный шаблон
(`loadMaterializeContext`), и автосохранение ОЛ записывает дерево, собранное
не тем шаблоном. Нужно: при ошибке контекст не собирается и не кешируется, ОЛ
сохраняет только форму с сообщением «шаблоны не загрузились — расчёт
пересоберётся позже». Встроенный шаблон — только если сервер ответил, что
своих шаблонов нет. Тест: запрос отклонён — дерево не сохраняется.

### 1.5 Отрицательные и неразобранные значения

- Количество и цена меньше нуля не принимаются, ячейка подсвечивается ошибкой
  (решение Р4). Сейчас «-5» в количестве уменьшает себестоимость, а в печатной
  спецификации такая строка скрыта.
- Неразобранное выражение в количестве — подсветка «не разобрано, в расчёте —
  расчётное» вместо тихого отката.
- Гейт выпуска КП (`POST /api/estimates/:id/kp`): строка с отрицательной
  суммой — 422.

### 1.6 Дата и номер КП

- Дата печатной формы — по часовому поясу `Europe/Moscow` (решение Р10).
  Сейчас — по часовому поясу контейнера (UTC), и КП, выпущенное до 03:00 по
  Москве, получает вчерашнюю дату. Проверить данные часовых поясов в образе
  `node:24-alpine`. Тест: снапшот в 22:30 UTC печатается следующим днём.
- Номер КП на проект — с датой или редакцией (решение Р11): сейчас
  переиздание получает тот же номер.

## Этап 2. Безопасность и доступ

- **2.1** Первый администратор: пароль из переменной окружения
  `ADMIN_INITIAL_PASSWORD`, без неё — случайный, показывается один раз;
  обязательная смена пароля при первом входе; сброс пароля пользователю
  администратором — с обязательной сменой и аудитом. Миграция `User`: признак
  обязательной смены и версия токенов (для 2.3).
- **2.2** Ограничение частоты входа и обновления токена — в приложении и в
  nginx, ответ 429.
- **2.3** Отзыв токенов при смене и сбросе пароля и при блокировке; «Выйти на
  всех устройствах».
- **2.4** Заголовки безопасности в nginx: CSP — сначала в режиме Report-Only,
  с `frame-ancestors` под Битрикс24. Шрифт Archivo — в сборку вместо Google
  Fonts. `index.html` — без кеширования.
- **2.5** Минимальная длина пароля — 8 символов везде; повтор email — 409;
  удаление расчёта — в аудит; демо-вход — только в dev-сборке.
- **2.6** По желанию: роль БД без прав суперпользователя, контейнер бэкенда не
  от root — с процедурой переезда для существующих установок.

## Этап 3. Согласованность и эксплуатация

### 3.1 Защита от записи поверх чужой правки

**Проблема.** `PATCH /api/estimates/:id/survey` склеивает `surveyData` без
проверки версии; защищён только случай «дерево старше ОЛ»
(`utils/survey-write.ts`). Долго открытый ОЛ перезапишет ручные правки,
сохранённые с экрана расчёта в другой вкладке; ревизия ОЛ может уйти назад.

**Что сделать.** Поле `Estimate.version`: запись увеличивает его, клиент
присылает версию, с которой работал; не совпало — 409. ОЛ на 409 перечитывает
расчёт, пересобирает и повторяет запись один раз; экран расчёта перечитывает и
просит повторить правку. Ревизия ОЛ не может уменьшиться.

### 3.2 Экран расчёта не теряет несохранённое

Сейчас правки экрана расчёта живут в памяти до кнопки «Сохранить»,
предупреждения при уходе нет. Автосохранение с паузой и статусом, как у ОЛ,
либо предупреждение при уходе (решение Р6).

### 3.3 Восстановление дампа не ломает запуск

**Проблема.** `pg_restore --clean` пересоздаёт только объекты из дампа:
таблицы и типы более поздних миграций остаются, а `_prisma_migrations`
откатывается — следующий старт падает на `migrate deploy`.

**Что сделать.**

- Перед восстановлением сравнить миграции в дампе и в коде: в дампе есть
  неизвестные коду — отказ; не хватает — предупреждение, какие применятся.
- Восстанавливать в чистую схему (`DROP SCHEMA public CASCADE`, затем
  `pg_restore`), после чего бэкенд перезапускается и применяет недостающие
  миграции с предмиграционным дампом.
- То же в `backend/scripts/db-restore.sh`.

**Проверка.** Временная база: восстановление дампа старой схемы, следующий
старт проходит.

### 3.4 Ротация дампов по видам

Предмиграционные дампы хранятся отдельно от ручных; дампы из админки
подчиняются тем же правилам.

### 3.5 По желанию: сервер сверяет итог КП

Сейчас итог КП — это `totals.salePriceRub`, присланный клиентом. После 1.3:
экономика на бэкенде, сторожится общими примерами (как `sn-rule.vectors.json`
и `depth.vectors.json`); при выпуске КП итог пересчитывается и сравнивается с
присланным (решение Р7).

### 3.6 Смена справочников видна в расчёте

Отпечаток справочников (нормы, веса, матрицы, Мс) и версий узлов каталога — в
дерево при сборке. Изменились — плашка «справочники изменились после сборки —
пересобрать», как у шаблона и редакции.

### 3.7 Роль BUYER

Сервер разрешает BUYER выгрузку заявки по любому расчёту, но экран заявки
грузит расчёт через `GET /api/estimates/:id`, а на чужой расчёт это 403;
списка расчётов у BUYER нет. Нужно: чтение всех расчётов и список для заявок
(решение Р8).

### 3.8 Мелочи

- Ручной выбор SN в ОЛ — только 5000 и 10000 (решение Р9). Сейчас
  предлагаются ещё 1250 и 2500, а для 1250 веса трубы нет.
- Старые адреса `/survey/kns`, `/survey/emk`, `/survey/kol` без проекта
  больше не создают расчёты, невидимые в интерфейсе.
- В «Формовку» и массу для ацетона — только операции в кг, а не любые строки
  в кг.
- `<html lang="ru">`; версия приложения в `/api/health`.

## Этап 4. Документация

### 4.1 Расхождения документации с кодом — ✅ 11.09.2026

Сразу, одним коммитом — то, что от исправлений не зависит:

| Написано | В коде | Где |
|---|---|---|
| Образец КП от заказчика не получен | Получен 09.09.2026 («КПВ6393»), НДС 22% | README, ТЗ §7, ROADMAP, ARCHITECTURE; комментарий к `GET /:id/kp/export` в `routes/estimates.routes.ts` |
| Фронтенд не вызывает `/api/pump-station` | ОЛ КНС вызывает `select-pump`, `discharge-pipe-diameter`, `pressure-piping` | ТЗ §4.2 (противоречит своим §7 и §9.7), README, DATA_FLOW |
| Четыре POST в `/api/pump-station` | Их пять, `/pressure-piping` не описан | README, ROADMAP, ARCHITECTURE, ТЗ §4.2 |
| Паспортная кривая насоса не реализована | Подбор идёт по ней | ТЗ §9.7 |
| `stores/estimates.ts`, `EstimateCard.vue`, `ContextMenu.vue`, `conflictsKept` | Удалены | ТЗ §5.1, §5.4, §8; ARCHITECTURE; DATA_FLOW |
| — | Справочник Мс и `PUT /api/templates/joint-layers`; причина снапшота CREATE, MANUAL, KP и 422 `KP_NOT_PRINTABLE`; `GET /api/prices/export` | Нет в ТЗ |
| Краны = (раб + рез) + 1 коллектор + 1 при аварийном | Задвижки напорной стороны = раб + рез + отводящие; клапаны = раб + рез; аварийная линия — в разделе 5 | ТЗ §5.6 |
| Сид заводит демо-пользователей всех ролей | По умолчанию — только администратора | ТЗ §8 |
| В админке две вкладки | Три, есть «База данных» | ТЗ §7 |
| Тестов 281/93, 266/128, 298/128 | 777 в 40 файлах и 251 в 15 | ARCHITECTURE, ROADMAP, План_реализации |
| Правила SN у фронтенда и бэкенда разошлись; гильзы продублированы | Сведены 11.09.2026; `buildSleeveNozzles` общий | ARCHITECTURE, ROADMAP |
| Репозиторий приватный | Публичный | РАЗВЁРТЫВАНИЕ |
| «Что предлагается делать дальше» (16.07.2026) | План исполнен | План_реализации §7 |

Вместе с пунктами этапов 0–3 — описания поведения, которое они меняют:

| Написано | Где | Пункт |
|---|---|---|
| «Сид идемпотентен, только досоздаёт» | README, РАЗВЁРТЫВАНИЕ | 0.1 |
| «Ручные — никогда молча» | Механика §8.3, §12.2 | 1.1 |
| «Прайс версионируется целиком» | README, ТЗ §3.1 | 1.2 |
| Ставки берутся из действующего прайса при каждом открытии | Механика §9, §12.3 | 1.3 |
| Порядок восстановления и отката | РАЗВЁРТЫВАНИЕ | 3.3 |

### 4.2 Ссылки на код — по имени, проверка в CI

В документах 138 ссылок вида «файл:строка»; из 77, которые можно проверить
автоматически, 45 указывают на место, где названного рядом символа нет в
пределах ±15 строк. Ссылаться на файл и имя функции; в CI — проверка, не
пропускающая номера строк в `README.md`, `РАЗВЁРТЫВАНИЕ.md`, `doc/*.md` и
`ntt-calculator/doc/*.md`. Исключения — первоисточник
`Реверс_калькуляторов.md` и исторические `План_реализации.md` и
`Промт_ОЛ_и_редактор.md`.

### 4.3 Устаревшие комментарии в коде

- «Блокирует переход CALC → REVIEW» (`engines/row.ts`,
  `utils/estimate-tree.ts`) — теперь это гейт выпуска КП.
- «Образец КП от заказчика не получен» (`routes/estimates.routes.ts`).
- «Поле ОЛ» у `DEFAULT_MIN_LEVEL_M` (`engines/survey-kns.ts`) — в форме такого
  поля нет.
- Текст отказа `LAST_ADMIN` (`routes/admin.routes.ts`) — после 0.5 и 2.1 он
  неверен.

## Открытые решения

До ответа Р1–Р10 выполняются по рекомендации; Р11 нужен только для 1.6.

| № | Вопрос | Рекомендация | Решение |
|---|---|---|---|
| Р1 | После первого запуска справочники ведутся только в приложении, новые данные — миграцией | Да | — |
| Р2 | Пароль первого администратора — из окружения или случайный; смена при первом входе обязательна | Да | — |
| Р3 | Каждая ручная правка цены — новая версия прайса | Да; альтернатива — пакетная публикация правок | — |
| Р4 | Отрицательные количество и цена запрещены | Да; скидка — через наценку | — |
| Р5 | Ставка экономики не найдена в прайсе | Блокировать выпуск КП | — |
| Р6 | Экран расчёта | Автосохранение, как у ОЛ | — |
| Р7 | Сервер сверяет итог КП | Да, после 1.3 | — |
| Р8 | BUYER видит все расчёты на чтение | Да, как в ТЗ §2 | — |
| Р9 | Ручной выбор SN | Только 5000 и 10000, по правилу завода | — |
| Р10 | Часовой пояс КП | Москва | — |
| Р11 | Формат номера КП на проект | Согласовать по образцу «КПВ6393» | — |

## Готовность

- Сервер поднимается после любых правок технолога и после восстановления
  старого дампа; упавший сервер делает деплой красным.
- Первый вход администратора требует смены пароля; пароли и секреты не
  выводятся в логи и ответы API.
- Правка ОЛ не теряет ручных правок молча: всё, что перенести не удалось,
  видно списком.
- Итог старого расчёта не меняется без действия инженера; версия прайса
  отражает любую правку цены.
- В документах нет расхождений из §4.1 и номеров строк.
