# Дорожная карта

> Актуализировано 2026-07-20. Прежняя редакция описывала спринты legacy-ветки
> (свободное дерево связок) — ветка удалена, история в git. Архитектура —
> `ARCHITECTURE.md`; поток данных — `DATA_FLOW.md`.

## Сделано

### Ядро расчёта
- [x] Расчётная библиотека `src/engines/*` — чистые функции, 263 юнит-теста,
      сверено с эталонными Excel (Механика §9, §13)
- [x] Материализация трёх изделий: КНС (7 разделов), ЕМК (8), КОЛ (7);
      общие узлы переиспользуются
- [x] Экономический хвост: 5 корзин, ПЗР/ацетон/СИЗ/накладные, наценка,
      цена продажи ROUNDUP до 100, рентабельность; ставки — позиции прайса
- [x] Тираж: итог за N корпусов + «за 1 корп.» отдельным прогоном

### Поток пользователя
- [x] Проекты → единый опросный лист `/survey/:id?` (ветвление по типу,
      создание с projectId, редактирование ОЛ существующего расчёта)
- [x] Конфигуратор `/calculator/:id`: overrides с «↺», конфликты
      «было → стало» при изменении ОЛ (рематериализация с переносом правок),
      строки без цены, каталог, свободные строки, фильтры, клавиатура
- [x] Выпуск КП: серверный гейт «нет строк без цены» + снапшот
- [x] Заявка на закупку: экран + выгрузка xlsx

### Платформа
- [x] Backend: JWT + RBAC, проекты/расчёты/прайс/справочники/админ, аудит,
      таблица переходов статусов, запись totalRub
- [x] Прайс: импорт xlsx (лист НН), версии прайса, история цен
- [x] Справочники в БД: веса труб GRP/ПЭ, матрица «Для расчетов», нормы патрубков
- [x] Docker Compose + verify.ps1; сид БД (пользователи, прайс 1043 позиции)

## Бэклог (по убыванию приоритета)

1. **Печатная форма КП** (`GET /:id/kp/export` — сейчас 501).
   Блокеры: образец документа от заказчика + решение «экспорт пока не
   требуется» (2026-07-20).
2. **Матрица масс ЕМК** (днища, шахта) — сейчас ручной ввод (`qtyCalc: null`);
   ждём данные завода (см. `doc/Вопросы_к_заводу` в корне репозитория).
   Матрица `ELLIPTIC_BOTTOM` уже редактируется в «Шаблонах» — при получении
   данных останется подключить её в `template-emk-kol`.
3. **Экспорт сметы в Excel** (ТЗ §7) — отложен решением 2026-07-20.
4. **Редактор шаблонов, этап 2** — если понадобится: редактирование состава
   разделов/узлов (сейчас структура в коде, данные в БД).
5. **Интеграция с Битрикс24**: приложение вкладкой «Расчёт» в карточке сделки
   (iframe ≥1000px). Роль VIEWER для этого уже готова (просмотр расчёта).
   Предусловие инфраструктуры: домен + HTTPS/TLS на сервере (iframe Битрикс
   требует валидный сертификат) — см. `РАЗВЁРТЫВАНИЕ.md`, блок «Перед
   интеграцией с Битрикс24». Дальше — SSO/проброс токена и привязка сделки
   к проекту/расчёту.
6. Мелочи: видимый маркер «ƒ» у расчётных значений (сейчас только tooltip),
   анимация панели итогов, `AppSidebar`/`AppTopbar` как общие компоненты.

Сделано из бэклога: история версий в конфигураторе + типизированные обёртки
snapshot/kp; **редактор шаблонов этап 1** (экран «Шаблоны» для TECHNOLOG:
нормы патрубков, веса труб GRP, обе инженерные матрицы — upsert/удаление с
аудитом); согласование REVIEW/APPROVED выведено из процесса (решение
2026-07-20, `STATUS_FLOW_REMOVED`).

## Технический долг

- Узлы «гильзы» и «теплоизоляция» реализованы в обоих template-модулях
  (КНС инлайн, ЕМК/КОЛ отдельными функциями). Остальной движок
  переиспользуется; блок PN/SN-override вынесен в `usePipeOverride`.
- Legacy-формат дерева в старых снапшотах: `backend/utils/estimate-tree.ts`
  обязан понимать обе формы, пока живы снапшоты до унификации.

Закрыто: `DeviceType` на фронте определён один раз (`types/device.ts`,
реэкспорты в api/estimates и template-kns; enum в schema.prisma — граница
систем, не дубль).
