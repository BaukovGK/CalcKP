# Каталог дампов БД. Сами дампы в git не попадают: это боевые данные
# (пользователи, прайс, расчёты), а весят они больше самого репозитория.
# Файл нужен, чтобы каталог существовал до первого запуска — том
# docker-compose монтирует его как ./backups.
